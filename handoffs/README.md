# ZECReveal 2.0 — handoffs (Aqua Stack v4.1)

L2 (Cowork) writes these. L3 (Claude Code: lead → director-build / director-quality → crews) executes **the lowest-numbered `open`** one, unless the prompt names a file — and the prompt names one whenever more than one track is open (LEDGER-02 Q1). The PR stops at opened; the operator merges. L2 verifies the preview/logs, writes back §7 / `LOG.md` / memory, and appends §8 to `LEDGER.md` — which L2 reads before writing the next handoff.

## Status convention
`open` = ready for L3 now · `queued` = written, not yet released (dependencies unmerged) · `in-progress` = an L3 session owns it · `shipped` = PR opened · `closed` = merged and written back. Exactly one handoff per track is `open` at a time; to run tracks in parallel, open one per track and tell each Claude Code session which file it owns.

Statuses are maintained by the sessions themselves; see "Revolution protocol" in CLAUDE.md.

## Branch convention (resolved after HANDOFF-00)
The harness names the branch a session is opened on, so handoff front-matter no longer prescribes one: use the session's designated branch, and name it `feat/v2-NN-<slug>` only when you are free to choose. The stable key is the PR title, which always begins `HANDOFF-NN:`.

## Kickoff line for a Claude Code session
```
Aqua Stack v4.1 session. Read CLAUDE.md, then handoffs/LEDGER.md, then execute the lowest-numbered handoff in handoffs/ with status: open. When more than one track is open I name the file, and that one is yours: handoffs/HANDOFF-NN-<slug>.md. If your handoff touches Redis, a Vercel variable or the publisher, read docs/2.0/SNAPSHOT.md first - the managed store is shared with an unrelated production project. Report spawn mode first. Stop at PR opened.
```

## Sequence

| # | Handoff | Branch | Track | Depends on | Status |
|---|---|---|---|---|---|
| 00 | [Housekeeping, docs import, CI runs tests, CLAUDE.md](HANDOFF-00-housekeeping.md) | `claude/aqua-stack-v4-1-handoff-818gb3` | Foundation — first | — | `closed` |
| 01 | [`apps/web` scaffold + the ZEC Forensic design system](HANDOFF-01-web-scaffold.md) | session-designated | Web | 00 (closed) | `closed` |
| 02 | [`packages/content` — zod schemas + research seeds](HANDOFF-02-content-package.md) | session-designated | Web | 00 | `closed` |
| 03 | [The Record — Splash, Beware, Contradictions, Timeline, Network, Method, Flows, Sources](HANDOFF-03-record-pages.md) | session-designated | Web | 01, 02 | `closed` |
| 04 | [ZEC Tracking UI in fixture mode — search, mempool, address, tx, pools, reveal](HANDOFF-04-tracking-ui.md) | session-designated | Web | 01, 02 (03 optional) | `closed` |
| 04a | [The legibility pass, and the turnstile plane](HANDOFF-04a-legibility.md) | `claude/legibility-pass-feedback-y5o07o` | Web | 01, 02, 03, 04, 09b (all closed) | `closed` (PR #47 `01abc2e`) |
| 04b | [The SVG text regime, and claim order on the remaining Record pages](HANDOFF-04b-svg-text-and-record-order.md) | session-designated | Web | 04a (closed) | `closed` (PR #48 `76ea9e7`) |
| 05 | [Gateway REST read API v2 + hardening (Zebra address-index RPCs with a cache)](HANDOFF-05-gateway-api.md) | `claude/gateway-api-handoff-05-12ogr3` | Data | 00 (uses the DTOs from 04 if merged; otherwise defines them) | `closed` |
| 06 | [Indexer: four pools + migration 003 + post-NU6.3 invariants](HANDOFF-06-four-pools.md) | `claude/new-session-s4er6f` | Data | 00 | `closed` |
| 07 | [Indexer: v6 / Ironwood decoder (module 7A.2) + migration detection](HANDOFF-07-v6-decoder.md) | `claude/new-session-ux5kkt` | Data | 06 (closed) | `closed` |
| 08 | [Indexer analysis toolkit: echo, clustering, labels, posterior, taint (+ golden cases)](HANDOFF-08-analysis-toolkit.md) | `claude/handoff-08-analysis-toolkit-bjvz3i` | Data | 06, 07 (both closed) | `closed` (4 rounds; PRs #39 `4386e98`, #40 `09b9e9c`, #41 `9393100`, #42 `4ae0796`) |
| 09 | [Turnstile accounting, migration lens, Ironwood birth, snapshot publisher](HANDOFF-09-instruments-snapshot.md) | `claude/new-session-e2vovd` | Data | 06, 08 (both closed) | `closed` (PR #44 `1f6e6dc`) |
| 09a | [The estimator package move - three instruments into a dependency-free workspace package](HANDOFF-09a-estimator-package.md) | `claude/new-session-ovtyx4` | Data | 09 (closed) | `closed` (PR #45 `730cf3f`) |
| 09b | [The two missing snapshot input sources - a block-time source and an Ironwood spend source](HANDOFF-09b-snapshot-inputs.md) | `claude/new-session-bfstws` | Data | 06, 07, 09, 09a (all closed) | `closed` (PR #46 `3225a5c`) |
| 10 | [Infra: Zebra 6.3.x compose, VPS runbook, tunnel, DEPLOY-2.0](HANDOFF-10-infra.md) | `claude/handoff-08-completion-wngbjj` | Infra | 00 | `closed` (PR #43 `8679e03`) |
| 11 | [Live wiring: snapshot baseline → WS upgrade, smoke tests, cutover checklist](HANDOFF-11-live-wiring.md) | `claude/new-session-zejoty` | Integration | 04, 04a, 04b, 05, 09, 09a, 09b, 10 | `closed` (PR #49 `fa696a6`) |
| 12 | [7B / 7C runtime wiring — PoolState replay, confirmed-block driver, assessments on the live path](HANDOFF-12-runtime-poolstate.md) | `claude/handoff-12-poolstate-reconcile-oyhra0` (PR #50) · `claude/handoff-12-reconcile-2becu3` (PR #51) · `claude/handoff-12-reconcile-2becu3` (PR #52, the gate fixes and the write-back) | Integration | 06, 07, 08 (all closed) | `closed` (PR #52 merged `98e87a0`; its §7 STATUS is DONE, which is what the F-50-4 rule requires before a merged PR closes a handoff) |
| 13 | [Mode A — viewing-key decryption in the browser (2.1; PLAN ONLY, stop for approval)](HANDOFF-13-mode-a-wasm.md) | `claude/new-session-0defoc` (PR #53 merged mid-gate; **PR #54** carries rounds 2 and 3; **PR #55** carries round 4 and the section 5 UNVERIFIED banner, merged `04237c5`) | 2.1 — plan only | 04, 11 (both closed) | `closed` (PLAN ONLY - the PRs carry `docs/2.0/MODE-A-PLAN.md` and deliverable 0's two guards; **it does not carry `packages/wasm-keys`, and the operator approves the plan before anyone writes it**) |
| 14 | [Live without a database - the publisher on RPC alone (rung 1 of three)](HANDOFF-14-live-without-a-database.md) | session-designated | Integration | 12 (closed) | `closed` (PR #56 `c12826a`) |
| 15 | [Live transactions - the mempool path on a rate-limited third-party endpoint (rung 2 of three)](HANDOFF-15-live-transactions.md) | session-designated | Integration | 14 (closed) | `closed` (PR #57 `f976477`) |
| 16 | [Crossings forward - the confirmed-block driver on a third-party endpoint (rung 3 of three)](HANDOFF-16-crossings-forward.md) | session-designated | Integration | 12, 15 (both closed) | `closed` (PR #58 `e10cbae`) |
| 17 | [The living tank - the turnstile plane subscribes to the live transaction stream](HANDOFF-17-the-living-tank.md) | session-designated | Web | 15, 16 (both closed) | `closed` (PR [#59](https://github.com/aqua-019/ZCashReveal/pull/59) merged `c32c46e`) |
| 18 | [The round-2 debt - three HIGHs in executable lines, and four assertions that pass against code that is wrong](HANDOFF-18-the-round-2-debt.md) | session-designated | Web | 17 (closed) | `shipped` (PR [#60](https://github.com/aqua-019/ZCashReveal/pull/60)) |

**18 IS `shipped`, AND IT IS THE FIRST HANDOFF IN THIS DIRECTORY OPENED BY ANOTHER HANDOFF'S NAMED DEBT.** 17 closes because PR #59 is merged into `main` at `c32c46e` **and because its section 7 STATUS is `DONE-WITH-ASSUMPTIONS` whose own first line reads "Nothing here is a partial build"** - that second half is F-50-4, and it is the test a merged PR has to pass before its handoff closes rather than a formality. `2326c84` - HANDOFF-17's gate-round-2 record, the commit that proves #59 landed whole and not merely its earlier commits - is `git merge-base --is-ancestor`-provable in `origin/main`, executed before any file was touched. **18 is created by the session that executes it**, the ninth in this directory to be written that way, and it keeps the Web track's active position rather than moving it. **No track has anything `queued`**, so step 1's "open the lowest-numbered `queued` handoff whose `depends_on` are all closed" again has no candidate on any track and correctly opens nothing. **AND ITS SCOPE IS NOT A NEW SURFACE**: 17's section 7 recorded three HIGHs in executable lines and four tests that do not discriminate, the operator merged knowing that, and this handoff is the remainder scoped from that written evidence rather than from memory - which is the process the stopping rule's bound exists to make possible.

Web (01→04), Data (05→09a) and Infra (10) are independent once 00 is closed. 00 through 10 are `closed` (PRs #31 `0031d7c`, #32 `0ed75ad`, #33 `8badaa3`, #34 `47ff9d5`, #35 `8d97e25`, #36 `31d5000`, #37 `eba5b03`, #38 `861c460`, 08's four at `4386e98` / `09b9e9c` / `9393100` / `4ae0796`, 10 at `8679e03`, 09 at `1f6e6dc`, and 04a at `01abc2e`). The Web track's first pass finished at 04 and the track REOPENED at 04a, which is the first Web handoff written after reader feedback rather than from the plan; 04b continues that reopening and is the second. The Infra track has nothing else queued - 10 was its only handoff - so it goes quiet until 11 integrates.

**17 IS `shipped`, AND THE RECONCILE THAT OPENED IT MOVED TWO CELLS.** 16 closes because PR #58 is merged into `main` at `e10cbae` **and because its section 7 STATUS is `DONE-WITH-ASSUMPTIONS` whose own first line reads "Every deliverable is in the tree"** - that second half is F-50-4, and it is the test a merged PR has to pass before its handoff closes rather than a formality. `2b63e1a` - HANDOFF-16's final write-back, the commit that proves #58 landed whole and not merely that its earlier commits did - is `git merge-base --is-ancestor`-provable in `origin/main`, executed before any file was touched. **17 is created by the session that executes it**, the eighth in this directory to be written that way, and it **REOPENS THE WEB TRACK** - quiet since 04b - rather than continuing Integration, because the thing it fixes is a surface and not a pipeline. **No track has anything `queued`**, so step 1's "open the lowest-numbered `queued` handoff whose `depends_on` are all closed" again has no candidate on any track and correctly opens nothing. **AND DELIVERABLE 0 CORRECTED FIVE ROWS OF ITS OWN BRIEF'S SECTION 1**, every one by reading the module rather than the citation: `TurnstilePlane` is a SERVER component whose own docblock forbids the `useEffect` the brief asked for; the browser's frame union is `views.ts:1048` and not the `realtime.ts` lines cited, which are the indexer-to-gateway seam carrying a different payload; `lanes` is an unordered set with no direction and a legal empty; `tx_removed` carries three reasons of which only one is `confirmed`; and `subscribeFrames` opens a socket per call, which is the whole reason `tip-bus.ts` exists. That is the third consecutive handoff whose section 1 needed correcting by execution, which is what its own instruction to check them exists for.

**16 IS `shipped`, AND ITS SECTION 1 WAS WRONG ABOUT THREE THINGS - WHICH IS WHAT IT ASKED TO BE CHECKED FOR.** A missing `z_gettreestate` does not cost an anchor, it STOPS the follower for ever at the first Ironwood block; the confirmed-block cost was thirty requests a minute against a ceiling of five, not 0.8, because the tip poll before every `getblock` was not counted; and crossings cannot reach the plane from the indexer alone, because the publisher is a separate process that queries Postgres for `migrationHist`. All three measured by execution. **Rung 3 therefore has two shapes** - one with a database that draws marks, one without that runs the follower and draws nothing - and `CUTOVER-1.0.md` section 10.1 states both in a table rather than implying the one that does not work.

**16 IS `in-progress`, AND THE RECONCILE THAT OPENED IT MOVED TWO CELLS.** 15 closes because PR #57 is merged into `main` at `f976477` **and because its section 7 STATUS is `DONE`** - that second half is F-50-4, and it is the test a merged PR has to pass before its handoff closes rather than a formality. `640865a` - HANDOFF-15's post-PR e2e record, the commit that proves #57 landed whole and not merely that its write-back did - is `git merge-base --is-ancestor`-provable in `origin/main`, executed before any file was touched. **16 is created by the session that executes it**, the seventh in this directory to be written that way, and takes the Integration track's active position; `depends_on` is 12 and 15, both now `closed`. **No track has anything `queued`**: Web 01 through 04b, Data 05 through 09b, Infra 10 and Integration 11 through 15 are all `closed` with nothing behind them, so step 1's "open the lowest-numbered `queued` handoff whose `depends_on` are all closed" again has no candidate on any track and correctly opens nothing. **AND ITS SECTION 1 CARRIES ONE UNVERIFIED ROW BY L2's OWN INSTRUCTION**: `z_gettreestate` answering `Method not found` on the keyless endpoint was measured on 1 September and not re-probed, so deliverable 1 settles it rather than the brief asserting it - F-56-1 pointed at the brief rather than at the probe, which is the correction L2 records against itself in the resolution for #57.

**15 IS `shipped`, AND THE RECONCILE THAT OPENED IT MOVED TWO CELLS AND NO MORE.** 14 closes because PR #56 is merged into `main` at `c12826a` **and because its section 7 STATUS is `DONE-WITH-ASSUMPTIONS` whose own first line reads "Nothing here is a partial build"** - that second half is F-50-4, and it is the test a merged PR has to pass before its handoff closes rather than a formality. `9553842` - HANDOFF-14's CI fix, the commit that proves #56 landed and not merely that its write-back did - is `git merge-base --is-ancestor`-provable in `origin/main`. **15 is created by the session that executes it**, the sixth in this directory to be written that way, and takes the Integration track's active position; `depends_on` is 14, which is now `closed`. **No track has anything `queued`**: Web 01 through 04b, Data 05 through 09b, Infra 10 and Integration 11 through 14 are all `closed` with nothing behind them, so step 1's "open the lowest-numbered `queued` handoff whose `depends_on` are all closed" again has no candidate on any track and correctly opens nothing. **AND DELIVERABLE 0 CORRECTED ITS OWN BRIEF'S SECTION 1.** L2's scope sentence said the mempool loop reads no Postgres; read against `c12826a` it reads Postgres for every shielded spend's anchor depth (`anchor-depth.ts:57`) and writes it for every added report (`index.ts:254`), off a `DATABASE_URL` with a localhost default. That is LEDGER-14's own composition-root finding standing in a second app, so "no database" became deliverable 6 rather than a footnote (LEDGER-11 Q5(a)).

**14 IS `shipped`, AND IT IS THE FIRST RUNG OF THREE.** The publisher runs against RPC alone with no `DATABASE_URL`: five live lane balances and a live unprovable-supply figure from one `getblockchaininfo`, and the three database-derived panels published as stated absences. `residual` comes back MEASURED, which the handoff's own brief said twice would be null - the correction is applied in its sections 4 and 5 rather than footnoted, on LEDGER-11 Q5(a). The `snapshot age: 0 blocks` a fixture-sourced page reported beside twelve-day-old data now reads `unknown`, and four places that encoded the old contract - two unit suites, the e2e spec and the primitives gallery - are corrected rather than waived. **HANDOFF-15 (live transactions) and HANDOFF-16 (crossings) are the rungs above and are not written yet**; nothing in this handoff reaches up the ladder. The operator's path is `docs/2.0/CUTOVER-1.0.md`, which is a DIFFERENT document from `CUTOVER.md` - that one is still the full destination and every variable this one sets is one it also sets.

**13 CLOSES, AND THE RECONCILE THAT OPENED 14 MOVED THREE CELLS PLUS THIS PARAGRAPH.** 13 closes because PR #55 is merged into `main` at `04237c5` **and because its §7 STATUS is `DONE-WITH-ASSUMPTIONS`, which says in its own words "Nothing here is a partial build"** - that second half is F-50-4, and it is the test a merged PR has to pass before its handoff closes rather than a formality. `4e622ff` - the section 5 UNVERIFIED banner - is `git merge-base --is-ancestor`-provable in `origin/main`, which is how a session knows #55 landed and not merely that #54 did. **Three cells were stale and all three for one reason: PR #55 never touched this file.** Row 13's branch cell named #53 and #54 and stopped; row 13's status cell read `shipped`; the front matter of `HANDOFF-13-mode-a-wasm.md` read `status: shipped`. **`LOG.md` was NOT stale and was not touched**: this directory keeps ONE ROW PER HANDOFF, updated in place, and #55 had already rewritten 13's row to carry rounds 4 and 5, the 91 agents a usage limit killed, and section 5's second verification failure. Reading its tail and seeing one row where two PRs merged is F-52-1's shape - an exhaustive claim made over the wrong object - and the object here is the handoff, not the PR. **14 is created by the session that executes it**, the fifth in this directory to be written that way, and takes the Integration track's active position as `in-progress`; `depends_on` is 12, which is `closed`. No track has anything `queued`, so step 1's "open the lowest-numbered `queued` handoff" again has no candidate and correctly opens nothing.

**13 IS `shipped`, AND THE RECONCILE THAT OPENED IT MOVED TWO ROWS AND NO MORE.** (This sentence read `in-progress` for one commit after the write-back flipped the row to `shipped` - the machine-readable field contradicted by the prose beside it, four lines apart, in the paragraph that names that shape. A gate reviewer found it.) 12 closes because PR #52 is merged into `main` at `98e87a0` **and because its §7 STATUS is DONE** - the second half is the F-50-4 rule doing its work rather than a formality, since it is exactly the test PR #50 failed. That PR carried the twelve defects three gate rounds found in the confirmed-block runtime onto the merged main, so the caveat the row below records is discharged: main now carries the treestate ordering fix, the anchor memo expiry and the compose default. 13 takes the `2.1 - plan only` track's active position as `in-progress`; this prompt names its file, which is the LEDGER-02 Q1 case, and it is also the only handoff left in the directory that is not `closed`. **No track has anything `queued`**: Web 01 through 04b, Data 05 through 09b, Infra 10 and Integration 11 through 12 are all `closed` with nothing behind them, so step 1's "open the lowest-numbered `queued` handoff whose `depends_on` are all closed" has no candidate on any track and correctly opens nothing. **AND 13 STOPS AT A PLAN.** Its PR carries `docs/2.0/MODE-A-PLAN.md` and two guards that have no other home; it does not carry `packages/wasm-keys`, because the approval gate this handoff exists to create is the operator's and a session that built the thing it was asked to design would have removed it.

**12 IS `shipped`, AND ITS MERGE LANDED ONE COMMIT SHORT OF ITS GATE.** PR #51 merged as `65bdac5` with second parent `5a3893b`, which is the commit before the gate-round-1 fixes; so `main` carries the confirmed-block runtime, the Ironwood anchor and the live-path assessments, and does NOT carry the twelve defects three gate rounds found in them - a treestate fetch that made one dropped RPC call fatal, an `onApplied` failure logged as a retry that never happened, a split walk that turned a database hiccup into a consensus diagnosis, an anchor registry left stale across a reorg, a compose default that opens a testnet base 705,857 blocks early, A4's missing data-mutation fail side, and then six more in the fixes for those - among them a callback catch that swallowed fatals, the same shape again one callback later, and a memo with no expiry that made this project's own claim about a 24-hour bound false. The follow-up PR carries all of them onto the merged main together with this handoff's write-back. Read LEDGER-12's Q13 before reasoning about what `main` does.

**HOW 12 GOT HERE, KEPT BECAUSE THE RULE CAME OUT OF IT (F-50-4).** PR #50 is merged into `main` at `4a77951`, and step 1 of the Revolution protocol says a handoff whose PR is merged goes to `closed`. Its own §7 says `STATUS: PARTIAL`: deliverable 0 and folds 1-10 landed, and §4 deliverables 1, 2 and 3 were never started. Closing it would have orphaned §4 silently - the standing kickoff line executes the lowest-numbered `open` handoff, and with 12 `closed` the next session would have gone to 13. So 12 goes back to `open` and, because this session executes it, straight to `in-progress`. The exception and its reason are in the reconcile commit message; CLAUDE.md step 1 now carries the rule (a merged PR closes a handoff only when its §7 STATUS is DONE, and a PARTIAL merge returns it to `open`); and this table's Status column agrees with the front matter, which it did not: the row read `shipped` beside a caveat saying the opposite in prose - a machine-readable field contradicted by the human-readable note next to it, which is this project's most-recorded defect shape, live in its own index. 13 stays `open` on its plan-only track. Every other track is quiet.

**12 TAKES THE INTEGRATION TRACK, AND 13 OPENS ON A TRACK OF ITS OWN AFTER SEVEN REVOLUTIONS QUEUED.** 11 closes because PR #49 is merged into `main` at `fa696a6`. 12 was already `open` on the Integration track and takes its active position as `in-progress`; this prompt names it, which is the LEDGER-02 Q1 case for naming a file when more than one is available. **13 opens, and the reconcile rule rather than a judgement is what opens it**: its `depends_on` are 04 and 11, 11 was the last of them still outstanding, and its track - `2.1 - plan only`, recorded in its own front-matter - is not the Integration track, so 12 being `in-progress` does not hold the one-open-per-track cap against it. It stays PLAN ONLY and stops for approval; opening it releases it to be written, not to be built. Every other track is quiet: Web 01 through 04b, Data 05 through 09b and Infra 10 are all `closed` with nothing queued behind them.

**THE TWO MAINNET CAPTURES REACHED THIS SESSION BY THE COMMITTED ROUTE AND `main` HAS MOVED PAST `fa696a6` BECAUSE OF IT.** Three web-UI commits sit on top of the PR #49 merge - `3505bfc`, `ee3fc7e`, `4515825` - and they place `mainnet-3432130-000000.json` and `mainnet-3441955-000000.json` under `docs/2.0/capture/`. That is the staging directory and not the fixture directory, so `block-decoder.test.ts`'s `mainnet-*.json` glob still matches nothing on `main` and its one test is still skipped there. Moving the two files into `apps/indexer/test/fixtures/blocks/` is HANDOFF-12's work and happens inside its PR, with the gate behind it.

**11 OPENS, AND IT IS THE FIRST TIME IN SIX REVOLUTIONS THAT THE LOWEST-NUMBERED `queued` HANDOFF IS NOT STEPPED OVER.** 04b closes because PR #48 is merged into `main` at `76ea9e7`, which was the last of 11's eight `depends_on`. Five revolutions running, 11 was stepped over because a handoff written *after* it had been inserted ahead of it - 09a, 09b, 04a, 04b - and the Web track's reopening is now finished. 11 takes the Integration track's active position as `in-progress`. 12 was already `open` on that track and stays open; this prompt names 11, which is the LEDGER-02 Q1 case for naming a file when more than one is available. 13 stays `queued`: it depends on 11, which is not closed. The Web, Data and Infra tracks are quiet - every handoff on them is `closed` and none has anything queued.

**And the reconcile is not the whole of what 11 owes before it wires anything.** Fold 1 of the L2 RESOLUTION for HANDOFF-04b makes reconciling 11's own §5 against the tree deliverable 0: the handoff was written on 22 August, and 04, 04a, 04b, 09, 09a and 09b have all shipped since, so several of its assertions were written against a tree that no longer exists.

**04b IS THE FOURTH HANDOFF WRITTEN BY THE SESSION THAT EXECUTES IT, AND THE FIRST WHOSE WHOLE SCOPE IS A PREVIOUS HANDOFF'S OWN §7.** 09a and 09b were consequences the plan implied and had not scheduled; 04a was a consequence the plan did not contain; 04b is the REMAINDER of 04a. Its two deliverables are the two sentences 04a wrote about itself - complaint 3 "closed for HTML text and MEASURED-OPEN for SVG chart text", and complaint 4 "closed on the SPLASH and open on the other seven Record pages". **That is the argument for a §7 that names what it did not close**: this directory's next handoff was written from it, verbatim, without a rediscovery pass. L2 ruled it owns itself rather than folding into 11 (LEDGER-04a Q3) on three grounds - the narrowest viewport is where most readers are and four diagrams still paint text at 8.64px there; the fix technique is already proven in 04a, which positions the turnstile plane's labels as HTML over the SVG for exactly this reason; and splitting complaint 4's remainder from complaint 3's would put two Web branches into `apps/web` at once, which is the collision 04a was ordered before 11 to avoid.

**The reconcile that opened it moved three rows.** 04a closes because PR #47 is merged into `main` at `01abc2e`. 04b is created and takes the Web track's active position as `in-progress`. **11 gains 04b in its `depends_on` and still does not open, which is the fifth revolution running where the lowest-numbered `queued` handoff is stepped over** - and for the second time running the reason is that a handoff written after 11 was written now blocks it. 12 remains the Integration track's one open handoff; 13 is plan-only and waits on 11. The Data and Infra tracks stay quiet: 05 through 10 are closed and neither has anything queued.

**04a IS THE THIRD HANDOFF WRITTEN BY THE SESSION THAT EXECUTES IT, AND THE FIRST WRITTEN FROM READER FEEDBACK RATHER THAN FROM THE PLAN.** 09a and 09b were both consequences the plan implied and had not scheduled; 04a is a consequence the plan did not contain. Four complaints from a reader, of which the fourth - "instead of claim, explanation, evidence, visualization, we get vibes, cryptographic terminology, vibes, huge number, tiny explanation, vibes" - is the diagnosis and the other three are symptoms. **The ordering argument is why it goes before 11 rather than after it.** HANDOFF-11 adds three more status affordances to these pages - a staleness indicator, a `source:` chip, an `UNVERIFIED` chip - onto a surface where a reader already cannot tell a button from a legend, so it would add to the exact problem it inherits. The hierarchy that says WHERE a status chip goes is therefore built first, and 11 receives that surface list as a design input. The letter follows 09a's precedent and for the same three reasons: LOG.md and LEDGER.md key on the PR title, the numbers 05 through 13 are already spent, and renumbering nine handoffs to insert one would invalidate every `depends_on` and every ledger cross-reference in the directory.

**The reconcile that opened it moved four rows.** 09b closes because PR #46 is merged into `main` at `3225a5c`. 04a is created and takes the Web track's active position as `in-progress` - the Web track had nothing open and nothing queued, so this is a track reopening rather than a queue advancing. **11 gains 04a in its `depends_on` and still does not open, which is the fourth revolution running where the lowest-numbered `queued` handoff is stepped over** - and this time the reason is new: 09b's merge closed the last of 11's original dependencies, so 11 would have opened on this reconcile had 04a not been written to block it. Both rewrite `apps/web` substantially and would conflict. 12 remains the Integration track's one open handoff; 13 is plan-only and waits on 11.

**09b is the SECOND handoff written by the session that executes it, and the reason is the same shape as 09a's.** HANDOFF-09a's principal finding was that wiring the real estimators un-nulls only two of the four analysis panels, because `drain` and `neffSeries` are missing an input SOURCE rather than a query - and LEDGER-09a Q1 asked whether the block-time migration becomes its own handoff or is carried by HANDOFF-11. L2 ruled 09b, **on a cost argument rather than on panel honesty**, and said so explicitly so the two would not be confused: migrations 003 and 004 have never been applied to the VPS, so that database is COLD, and a 005 landing now is applied in the same first `migrate` run the operator has owed for three handoffs - three migrations, one cold run, zero downtime - where a 005 landing after the cutover is a maintenance window on a live public site. In the same resolution L2 corrected its own honesty rule: HANDOFF-11's "may not ship a null analysis panel" was stated on the wrong quantity and becomes **"may not RENDER AN UNMEASURED PANEL AS A MEASUREMENT"**, which is count-independent and which - L2 noted against its own interest - no longer blocks the cutover, provided each absent panel renders as a named absence stating the CONDITION that produced it. (L2's wording was "carrying its owner"; `docs/2.0/SNAPSHOT.md` section 8.1 superseded that half in gate round 4, because an owner is a live statement on the wire and a prediction that outlives its subject reads as a fact.)

**The reconcile that opened it moved three rows.** 09a closes because PR #45 is merged into `main` at `730cf3f`. 09b takes the Data track's active position as `in-progress`. 11 gains 09b in its `depends_on` and still does not open, which is now the third revolution running where the lowest-numbered `queued` handoff is stepped over for the same reason: the rule opens the lowest-numbered `queued` handoff on a track *whose `depends_on` are all closed*. 12 remains the Integration track's one open handoff; 13 is plan-only and waits on 11.

**09a is the first handoff in this directory written by the session that executes it, and the first with a letter rather than a number.** Both are consequences of LEDGER-09 Q4. That question asked who moves the three estimators out of `apps/indexer/src/analysis/` and when, since the publisher's image structurally cannot contain them and so publishes four `null` analysis panels on every tip. L2 ruled: its own handoff, and it goes BEFORE HANDOFF-11, because `SnapshotV1`'s `null` is the honest TYPE and does not make a null PANEL honest on a production page - the LEDGER-05 Q2 precedent, where `/api/pools` answers 503 naming the four blocks it cannot serve rather than serving four empty ones. That makes the move a PREREQUISITE of the cutover rather than a sub-task of it, and a prerequisite folded into the handoff it blocks is a prerequisite that gets cut when the gate runs long. The letter rather than `10a` or a renumber is deliberate: LOG.md and LEDGER.md key on the PR title, the numbers 10 through 13 are already spent, and renumbering four handoffs to insert one would invalidate every `depends_on` and every ledger cross-reference in the directory. It costs nothing on the critical path, which is what settled it: HANDOFF-11's cutover is already blocked on operator hardware - the VPS is not provisioned, the runbook has not been run, the tunnel does not exist, and migrations 003 and 004 have never been applied.

**The reconcile that opened it moved three rows.** 09 closes because PR #44 is merged into `main` at `1f6e6dc`. 09a takes the Data track's active position as `in-progress`. **11 still does not open, and that is the part worth reading twice**, because it is the second revolution running where the lowest-numbered `queued` handoff is stepped over: the rule opens the lowest-numbered `queued` handoff on a track *whose `depends_on` are all closed*, and fold 5 of the L2 RESOLUTION for HANDOFF-09 adds 09a to 11's `depends_on` for exactly the reason above. 11 therefore waits on a handoff that did not exist when it was written. 12 remains the Integration track's one open handoff. 13 is plan-only and waits on 11.

Kept from the previous revolution because it explains the shape of `LEDGER.md` and `LOG.md`: HANDOFF-08 and HANDOFF-10 ran concurrently on separate branches, 08's session owned its own flips, §7, LEDGER §8 block and LOG rows and 10's session owned 10's, and the two collided on this table and on the two append-only files when 08's PRs #40 and #41 merged first. Resolved by keeping BOTH records rather than either: the ledger and the log each carry 08's blocks and rows followed by 10's, in that order.

## Human clicks (L4 — nothing here is done by an agent)
| When | What the operator does |
|---|---|
| before 00 | **one upload**: copy `2026-08-22-pickup/` into the repo root as `_incoming/` (everything HANDOFF-00 needs is inside — docs, research, mockups + reference screenshots, the stack diagram, `CLAUDE.md.draft`, `v0.2-notes/`, `handoffs/` with `LEDGER.md`/`LOG.md`); HANDOFF-00 moves it into `docs/2.0/` + `handoffs/` and `_incoming/` disappears |
| every handoff | review the PR → merge → mark the handoff `closed` in its front-matter + `LOG.md` |
| after 00 | run the commands in `docs/2.0/BRANCH-CLEANUP.md` to delete the stale remotes, including `claude/build-leak-panel-I0181` — L2 reviewed it: an early 748-line LeakPanel superseded by the 527-line version on main, in an app that HANDOFF-11 has now deleted from the tree |
| 01 — done | create the Vercel project `zecreveal` (Root Directory `apps/web`, Framework Next.js) from the repo. Done 23 Aug 2026: `prj_rNTLvGWnz92w5qcvROBchPUfdhIR`, and both v0.2 projects were deleted the same day, so it is now the only project on the account |
| 02 | **clear the stale build overrides on the `zecreveal` project** (Settings -> Build & Development): Framework Preset `Next.js`, and Build Command, Install Command and Output Directory overrides OFF. `apps/web/vercel.json` overrides them so the build no longer depends on this, but the legacy dashboard's build command is still stored on the new project and is a trap for whoever next edits that file |
| 03 — **withdrawn** | ~~turn on Protection Bypass for Automation on the `zecreveal` project~~ — **L2 has withdrawn this request** (LEDGER-04 Q3). HANDOFF-04 found a second wall behind Deployment Protection: the session container's egress proxy refuses the preview host outright (`curl` returns `CONNECT tunnel failed, response 403`, not a 302 to SSO), and L2 reproduced the same class of refusal from its own side. Lifting Deployment Protection therefore would not make a preview measurable by any session, so it is no longer worth toggling for L2's or L3's benefit — leave it as you prefer. The ruling that replaces it: the container Lighthouse number is the gate a session reports; the deployed number is the operator's, taken in a browser and pasted into the ledger. If you want one on the record, measure `zecreveal-git-main-aquatic-17b9f112.vercel.app/beware` yourself and paste the performance and accessibility figures |
| 03 — done | ~~rule on the two open questions in LEDGER-03~~ — answered in the L2 RESOLUTION for HANDOFF-03: the performance floor moves to the deployed page and a Record page of `/beware`'s size passes at 90 where no deployed measurement is reachable; gold has four licensed jobs, the fourth being the system-identity register. Both are folded into `CLAUDE.md` and HANDOFF-04 |
| 09 → 11 — **done 23 Aug 2026** | ~~connect the Marketplace **Redis** store to `zecreveal` and copy the names across~~ — the store `upstash-kv-blue-garden` is connected for Production and Preview under the variable prefix `SNAPSHOT_REDIS`, so Vercel injects the five names automatically and there is nothing to copy on that side. Two things remain yours: paste the `rediss://` TCP URL into the VPS `.env` when the publisher ships (HANDOFF-09), and **read `docs/2.0/SNAPSHOT.md` before touching that store by hand** — it is shared with an unrelated production project, and the destructive commands are forbidden there for that reason |
| 08 — **two PRs, and the second is the one that matters** | PR #39 was merged at 14:54 UTC on 29 Aug while gate round 1 was still running, so `main` at `4386e98` carries HANDOFF-08 **without** its gate fixes and without the write-back. The follow-up PR carries both, including the fix for a HIGH finding that is live on `main`: `matchEcho` lets one deposit answer several withdrawals, so a single 100 ZEC deposit and three 100 ZEC withdrawals in one window produce 300 ZEC of estimated exits against a pool that held 100 - a direct contradiction of TRACKING-MATH section 3.11. **Nothing downstream should capture golden cases or build on the analysis toolkit until the follow-up lands.** Review it, merge it, then mark 08 `closed` |
| **standing - TWO OF ITS FOUR PARTS ARE NOW CLOSED, MEASURED** | **CAPTURE A MAINNET BLOCK WITH A JOINSPLIT, AND A TESTNET BLOCK.** `docs/2.0/RUNBOOK-VPS.md` section 10 carries the procedure. This row used to read "CAPTURE THE MAINNET BLOCK FIXTURE", and said no session could do it because none can reach a synced node. **Four captures landed in PR #51 from a PUBLIC endpoint** - `mainnet-3432130-9eb351`, `-3441955-54b709`, `-3444836-1e5057`, `-3444837-274151` - so the premise was wrong and the row is corrected here rather than left standing (LEDGER-03 Q3: a corrected fact is swept, and a live instruction telling an operator to do something already done is the same defect as a stale status cell). Of the four things it named: **(1) the `skipIf`-guarded fixture test - CLOSED.** The decoder suite runs 11 of 11 with zero skips. **(3) the `trees.ironwood.size` observation - CLOSED.** All four captures carry it: 13,640 / 40,155 / 48,467 / 48,470. **(2) the `vjoinsplit` end-to-end path - STILL OPEN**, and this is what remains yours: all four captures carry ZERO `vjoinsplit` entries, measured by reading them, so the Sprout value terms are still INDETERMINATE and nothing has yet turned one "observed". **(4) the testnet half of the ZIP 258 exposure - STILL OPEN**: all four are mainnet, so `IRONWOOD_HEIGHTS_REST_ON_A_DRAFT_ZIP` stands. Neither remaining half needs a synced node either, on PR #51's own evidence - it needs a block that HAS a JoinSplit, which is rare rather than unreachable. |
| **09 — after one full month of publishing** | **READ THE MANAGED STORE'S ACTUAL COMMAND COUNT.** The publisher issues three `SET`s per tip inside one `MULTI`, so **five** commands cross the wire and three of them write. Whether Upstash's meter bills `MULTI` and `EXEC` is a fact about their billing that **no session can read** - egress to `upstash.com` is refused by the container's proxy. **L2 could reach it and ruled on 30 Aug 2026 (LEDGER-09 Q2, fold 2): charge five and raise the ceiling.** Upstash's pricing page publishes an exemption list - "AUTH, HELLO, SELECT, COMMAND, CONFIG, INFO, PING, RESET, and QUIT are not charged" - and `MULTI` and `EXEC` are not on it. That is evidence rather than proof, and the tie is broken toward the shared allowance because it is the other project's. So `apps/publisher/src/budget.ts` now charges `WIRE_COMMANDS_PER_TIP` (five, about 172,500 a month) against a default ceiling of 200,000, and pins `COMMANDS_PER_TIP` (three) beside it because A10 asserts the write count by counting. **What is left for you is the bill:** compare the console's monthly command count against the tips published. **Do not overwrite either constant with it** - `COMMANDS_PER_TIP` (3) and `WIRE_COMMANDS_PER_TIP` (5) are measured facts about what the code does, both pinned by tests, and a meter reading is a third quantity. What changes is the CHARGE: the `redis` sink's `managedStoreCommandsPerWrite`, which is `WIRE_COMMANDS_PER_TIP` today and becomes `COMMANDS_PER_TIP` if the bill says the envelope is exempt. Then re-check `SNAPSHOT_REDIS_MONTHLY_BUDGET` against it. |
| **09 — done, with one thing to confirm** | ~~review PR #44 and merge~~ — merged at `1f6e6dc`, and HANDOFF-09 is `closed`. **L2 could not read CI's conclusion on `94ea20b` from its side** — the checks page has not rendered a verdict for it on the last three PRs — so confirm the tick yourself. L2's local figures on a clean worktree: 1204 passed / 2 skipped, eleven guards, typecheck 10/10, lint 0/0 |
| **09b — one cold run, and doing it BEFORE the cutover is what keeps it free** | **APPLY MIGRATIONS 003, 004 AND 005 TO THE VPS DATABASE, IN ONE RUN.** `pnpm --filter @zcashreveal/indexer migrate` applies whatever is missing, in filename order, each inside its own transaction; on a database that has never had them it is one command and zero downtime. **This is the whole reason HANDOFF-09b was ordered before HANDOFF-11.** That database is COLD - 003 and 004 have been owed for three handoffs and nothing has been written there - so all three cost one run. The same three applied AFTER the cutover are a maintenance window on a live public site holding real state, because 005 adds a table and a column that the publisher reads on every tip. L2 put the ordering on this cost argument alone and said so explicitly, so it is not confused with the panel-honesty rule it corrected in the same resolution: the honesty rule no longer blocks the cutover, and this does not block it either - it just gets very much more expensive if you leave it. 005 is re-runnable IN ITS DDL, proven by applying it twice and diffing the schema - so a partial or repeated run is safe. **Apply it from the MERGED tree, not from a branch checkout.** `IF NOT EXISTS` makes a second application a no-op, and a no-op includes not applying later corrections: a database that ran a pre-merge draft never receives `blocks_height_check`, keeps the `blocks_hash_idx` a later draft deleted, and keeps a non-partial `pool_nullifiers_anchor_idx` - because `schema_migrations` keys on the FILENAME and cannot see the difference. Those three need correcting by hand there. |
| 10 | provision the VPS, run the runbook, create the tunnel |
| 11 | **the checklist is written: `docs/2.0/CUTOVER.md`.** Read it top to bottom before starting - it names three things that do NOT block the cutover (the mainnet block fixture among them), the preconditions in order, the Vercel variables that are already injected and must not be copied by hand, the promotion click, and eleven verification checks that are all UNVERIFIED here because no session can reach a deployed host |

Redis is two instances on purpose: the VPS Redis (`REDIS_URL`) carries the hot path (pub/sub, `zcashreveal:mempool:live`, anchors) and never leaves the box; the managed Redis is **shared with an unrelated production project**, and this repository owns one namespace in it, `zecreveal:snapshot:*` (3 writes per block), so the public site can render from it when the VPS or the tunnel is down. Nothing before HANDOFF-09 needs the managed store — but **read `docs/2.0/SNAPSHOT.md` before any change that touches Redis, a Vercel variable or the publisher**: because that store holds someone else's live data, its rules (the namespace, the forbidden commands, the shared budget) are not negotiable by a handoff, and CI enforces the command list.

## Files
- `LEDGER.md` — the §8 ledger, append-only, read before every new handoff.
- `LOG.md` — one line per revolution: date · handoff · PR · status · gate rounds.
- `HANDOFF-NN-*.md` — the handoffs themselves (this README is the index).
- `prompts/PROMPT-NN.md` — the prompt that started each session, archived verbatim.
