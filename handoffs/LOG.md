# LOG — ZECReveal 2.0 revolutions

| date | handoff | PR | status | gate rounds | notes |
|---|---|---|---|---|---|
| 2026-08-22 | — | — | handoffs 00–13 written by L2 | — | 00 open, rest queued |
| 2026-08-22 | 00 housekeeping | [#31](https://github.com/aqua-019/ZCashReveal/pull/31) | shipped (PR opened, CI green) | 0 | docs to docs/2.0, handoffs to root, CLAUDE.md 2.0, CI runs 178 tests on postgres:16 (run 32603472860 SUCCESS: indexer 170 passed/1 skipped, gateway 7 passed), eslint flat config, dashboard parked under legacy/. A5 corrected: 1 test is fixture-gated, not Postgres-gated. Delivered on branch claude/aqua-stack-v4-1-handoff-818gb3, not feat/v2-00-housekeeping. |
| 2026-08-22 | 00 housekeeping | [#31](https://github.com/aqua-019/ZCashReveal/pull/31) | closed (merged 0031d7c) | 0 | L2 re-executed all nine assertions on a clean worktree of 514ae4c: all hold, transcripts accurate. CI green on the PR head (run 32603703571). Vercel red check confirmed pre-existing on main. Ledger questions 1-4 answered; folds applied to 01, 05, 06, 10. HANDOFF-01 opened. |
| 2026-08-23 | 01 web scaffold | [#32](https://github.com/aqua-019/ZCashReveal/pull/32) | shipped (PR opened) | 2 | apps/web scaffolded: Next.js 15.5.23, React 19, Tailwind v4, tokens harvested from the mockup, 19 primitives, 3 ambience components, 9 routes + OG + dev gallery. 96 vitest + 19 Playwright tests. All eight section 5 assertions carry two-polarity transcripts. Lighthouse on /beware: performance 99, accessibility 100. Two gate rounds: the dev-surface gate failed open, the splash pool bar collapsed to 2px slivers, .fair was applied and undefined, and two ink tokens failed WCAG AA on body text. LEDGER-01 Q1 asks L2 to rule on the ink-token divergence from the mockup. |
| 2026-08-23 | 02 content package | [#33](https://github.com/aqua-019/ZCashReveal/pull/33) | shipped (PR opened) | 4 | packages/content: zod schemas, typed loaders, a validator wired into CI, and ten seeds transcribed from the dossiers. beware 14, contradictions 16, timeline 124, network 36 entities / 41 edges, phrases 19, labels 8, cases 3, unverified 32, sources 328 with 186 cited. sources.json is generated from the corpus and every url is provenance-checked against docs/2.0/research; citations are written as urls and resolved to ids by script, so a citation from outside the corpus cannot survive. All seven section 5 assertions carry two-polarity transcripts. 58 vitest tests. Four gate rounds: the timeline was missing the whole promotion network and carried a corrected ticker in a title, two network exposures outran the corpus, and a Form 144 was attributed to the wrong Silbert entity. LEDGER-02 Q6 asks L2 to rule on the fourth round, which overruns the Loop 4 cap deliberately. A second L2 RESOLUTION arrived mid-session: the root vercel.json is deleted (it applied to every project in the repo and broke zecreveal's first build with NEXT_OUTPUT_DIR_MISSING), its settings recorded in DEPLOY-2.0.md for the operator to move into z-cash-reveal-dashboard2, and a new assertion A8 with a CI check stops it coming back. The deployment on the deleting commit then failed identically: the same settings are stored on the zecreveal project too, so apps/web/vercel.json now pins framework, install, build and output, which take precedence over stored project settings. GitHub Actions green throughout (run 32611476921), and the deployment on the pinning commit succeeded: dpl_CrmijWHtimk842w65Tkux7vibZg1 READY in 19 s, twelve routes prerendered. |
| 2026-08-23 | 03 record pages | [#34](https://github.com/aqua-019/ZCashReveal/pull/34) | shipped (PR opened) | 4 | The Record, rendered from packages/content: eight surfaces, 24 new components, the citation apparatus on every claim (id, confidence, absolute canonical URL, sources), four inline-SVG charts each with a table twin, /beware.xml as RSS 2.0 and ten prerendered JSON exports. Fonts vendored to next/font/local, so a build inside an empty network namespace reaches Generating static pages - the flake that blocked HANDOFF-10's Playwright job is gone. pnpm -r test made self-sufficient (L2 finding F-02-1), and the first fix for it raced two concurrent tsc -b, which is why the suites now resolve the types package to source. Ten of eleven section 5 assertions hold with two-polarity transcripts; A5 is accessibility 100 (floor 95) and performance 94 (floor 95), taken from 89 by four real reductions and then reported rather than shaved - LEDGER-03 Q1 asks L2 to rule on a floor set against a two-row placeholder. Four gate rounds. Two reviewers returned 37 findings: the Silbert Form 144 was still misattributed in contradictions.json and network.json after HANDOFF-02 corrected only timeline.json, the Grayscale custodian was crossed with Cypherpunk's, Zooko Wilcox's compensation was stated two ways on two pages, a <details> inside a <p> broke hydration twice in one session, six --ink-faint text uses failed AA, and the block-arrival ceremony was running on every Record page while A6 passed for the wrong reason. Six defects were in the session's own tests. 68 Playwright, 337 unit tests, all workspace gates green; Vercel preview READY on the branch head, which is the first evidence the pinned build command handles the new workspace dependency. |
| 2026-08-23 | 04 tracking ui | [#35](https://github.com/aqua-019/ZCashReveal/pull/35) | shipped (PR opened) | 1 | The Tracking suite in fixture mode: seven routes (/track with search and a live mempool panel, /address, /tx, /block, /pools, /reveal, /track/flows), the nine-member ZecApi with a fixture implementation and an unused HttpApi against the same interface, every view DTO as a zod schema in packages/zec-types, and a fixture corpus whose every aggregate is derived from its own components - which surfaced four self-contradictions in the mockup, each corrected with a stated reason. Deliverable 1 first, per LEDGER-03 fold 6: three preformatted treatments, two compact-cell registers and seven card insets collapsed, held by A9 at maxDiffPixels 0 on /beware and /flows. Deliverable 9: Unverified.surface on the content schema, permalink() reading it, and apps/web's split module deleted. /track's client bundle taken from 217 kB to 115 kB by two empirical findings - a barrel import and zod on the streaming path. A11 and A12 were added mid-session by the operator. A11 measures the viewing-key promise from outside the browser and is what found that React serialises a controlled input's value into the DOM ATTRIBUTE; both key-capable fields are uncontrolled and the page now ships a CSP with connect-src 'self' and form-action 'none'. A12 is the deployed preview: dpl_J5ryna9fz6mh2TUgZmPTXWxSLNVF READY in 31 s, against dpl_Aca2Y5zfqgi2H2UFX4BbhTEgsEf5 ERROR on the previous commit with "Module not found: Can't resolve '@zcashreveal/types'" - vercel.json now builds through turbo and check-vercel-config.mjs asserts that form. One gate round, four reviewers, all FAIL, 36 findings: two HIGH claims the site's own arithmetic contradicted (a hardcoded 'broad' claim level where claimLevelFor says aggregate_only, and an EstimateCell rendering a claim chip with no assumptions), a ZIP 1015 coinbase split the corpus contradicts by a factor of 3.3, three absolute non-existence claims the Record itself refuses to make, 'Powers of Tau' attributed to Sprout, a tolerance factor off by twenty, the Mode B fog painting text at 1.18:1, and gold on five surfaces none of its four jobs licenses. Lighthouse after the gate: accessibility 100 on all seven routes, performance 95-97 except /address at 94, which is the audit trail the gate required. 127 Playwright and 548 unit tests green. LEDGER-04 asks L2 six questions; Q3 reports a second wall in front of the deployed measurement - this container's egress proxy refuses the preview host, independently of Deployment Protection. |
| 2026-08-23 | 05 gateway api | [#36](https://github.com/aqua-019/ZCashReveal/pull/36) | shipped (PR opened) | 3 | The read API v2 on the Fastify gateway: twelve route modules at BOTH `/api` and `/v2` (the handoff says one, HANDOFF-04's only written client sends the other, and mounting one would have broken the other at cutover - LEDGER-05 Q1 asks which survives), every response validated against its zod DTO before it is serialised, labels and cases served from packages/content with the labeller and precedence rank travelling with every label, a Postgres cache-aside on migration 003a, `@fastify/rate-limit` per IP, a WS cap closing with 1013, and `packages/zebra-rpc` - a typed, retrying, zod-validated client written against Zebra 6.3.0's STRUCTS rather than its doc comments, four of which contradict its own serialisation code. `apps/indexer/src/zebrad-rpc.ts` deleted. `/api/pools` answers 503 naming the four blocks HANDOFF-06 to -09 own rather than fabricating them; `/api/snapshot` answers 501. Assertion A9 was added mid-session by the operator after the session's own 404 finding: a pino serialiser that logs the method, the request id and the PATH, drops the query string entirely and redacts anything viewing-key-shaped in what remains, asserted over a captured log stream. The other operator message found that the wallet fingerprints were INERT, not degraded - `RpcTransaction` declared `expiryHeight` and every node sends `expiryheight`, so `expiryDelta` was null for every transaction ever processed and the existing tests passed vacuously over hand-written fixtures. A real-shaped fixture and a casing lint now exist; the fix revives YWALLET and ZECWALLET_LITE, and NIGHTHAWK and ZCASHD_RUST stay blind because the fee they gate on is also absent from the wire. Three gate rounds: round 1 (lead, 18 findings) inverted pool-delta sign, a gross figure published as net, ZIP 317 computed three ways wrongly, `summary.bytes` hardcoded to a zero the site renders as "0.0 kB", a 404 handler echoing the query string, and a planted ten-fold error in the zatoshi conversion that the suite did not catch. Round 2 (Workflow, four lenses, all FAIL, 39 findings) found the rate limiter was not per-reader behind a proxy at all, Sprout omitted from the value balance, Orchard actions double-counted, a conventional-fee count built from the wallet guess rather than the fee, and an IEEE-754 justification the session itself had published that is arithmetically false - swept across all six restatements including HANDOFF-04's fixture. Round 3 read the 19 HIGH/MID findings round 2 returned UNVERIFIED against an undeclared cap: two were still live and both were real. 704 unit tests green (gateway 90, zebra-rpc 23, content 67, web 346, indexer 178/1 skipped), typecheck 10/10, lint 0 errors. Postgres 16 was started so the gated integration test would run rather than be reported as a skip. LEDGER-05 asks L2 six questions; Q3 reports that `docs/2.0/TRACKING-MATH.md` §3.5 and /method state a ZIP 317 formula that diverges from the protocol exactly on the ZIP 271 lockbox, the one address the project exists to track. A THIRD operator message arrived after PR #36 opened - the L2 NOTE that the Vercel-managed Redis is connected AND is SHARED with an unrelated production project - and was applied to the same branch under its own instruction. It corrected the repository as well as constraining it: the five variable names Vercel actually injects under the `SNAPSHOT_REDIS` prefix are not the three this tree stated in thirteen places, and HANDOFF-11's SnapshotStore keyed its whole resolution order on two names that are injected by nothing, so code built to that spec would have read undefined, fallen through to the fixture and rendered a stale site reporting no fault. New: `docs/2.0/SNAPSHOT.md` (the topology, the `zecreveal:` namespace rule, the forbidden commands, the shared budget, the operator's exit condition), `scripts/check-redis-safety.mjs` (20 self-tested detectors, in CI and `pnpm check`), and `packages/zec-types/src/redis-topology.ts` (both prefixes as constants plus `assertNotManagedStore`, which the gateway and the indexer now use to REFUSE TO START if a Redis URL they would dial is the managed store - by host and by exact value match). An adversarial sweep of 37 agents over the whole tree found what the first pass missed: `UNLINK` defeated the no-DEL-by-pattern rule by one word, `redis-cli --scan/--bigkeys/--hotkeys/--memkeys/--rdb` bypassed both enumeration detectors and are the form a runbook uses, every rule was key-scoped so MONITOR/RANDOMKEY/DBSIZE/INFO keyspace fell outside all of them (now rule 7, because sharing a database is a confidentiality problem in both directions), a self-test fixture passed by matching its own explanatory comment rather than the call it certified, the whole command budget counted only writes while `apps/web`'s reads are the unbounded half, and HANDOFF-11 §3 specified `@upstash/ratelimit` on the shared store's read-write credentials - a per-request writer, outside the namespace, in another company's database - which is struck and pointed at the gateway's own limiter. LEDGER-05's addendum asks four more questions, including who owns the read side of the budget and whether Preview should stay connected. |
| 2026-08-23 | 06 four pools | [#37](https://github.com/aqua-019/ZCashReveal/pull/37) | shipped (PR opened) | 2 | The pool model widened from two to four across types, state, persistence and analysis. `ShieldedPool` is sprout|sapling|orchard|ironwood and stays the single source of truth: `Pool` aliases it, `poolNameSchema` mirrors it, migration 003 mirrors it in five CHECK constraints, and views.ts now carries a compile-time assertion that the type and the zod enum are mutually assignable - checked in BOTH directions, because one alone passes while one union is a subset of the other, which is exactly the state this repository was in. The widening produced ZERO compile errors in apps/, which is the finding rather than the relief: the display layer already spoke four pools, the domain layer did not, and nothing connected them. Sprout was invisible - its movement is a JoinSplit sum, not a valueBalance field, so a Sprout transaction had a boundary of exactly zero and was classified transparent throughout; `vjoinsplit` is now a declared field rather than four `as unknown as` reads. Orchard is exit-only from NU6.3 and `ValuePool.apply` throws `ExitOnlyViolation` on value entering it, network-aware, activation-inclusive. THE FEE NOW EXISTS: no node sends one, so `BigInt(tx.feeZat ?? 0)` was `0n` for every transaction this project ever analysed, and since ZIP 317's conventional fee has a floor of 10,000 zatoshi, two wallet signatures were not degraded but INERT with every test passing vacuously. `computeFeeZat` sums the spent outputs through an injected resolver and returns null rather than a partial sum; `PrevOutCache` supplies it on the live path, bounded by retained outputs after the gate measured 420 MB at the old entry-count bound. ZIP 317 had FOUR implementations that disagreed; one is canonical now, taken from Zebra, and TRACKING-MATH 3.5 and /method state the byte rule with the count form beside it and the ZIP 271 lockbox worked out (two 2-of-3 P2SH inputs: L=4 and 20,000 zat against the count form's L=2 and 10,000). Migration 003 widens five CHECKs, adds pool_snapshots and migrations_zip318, adds a Sprout balance column, drops fee_zat's NOT NULL DEFAULT 0 AND rewrites the rows already carrying that false zero; the runner is transactional per migration; `premigrate` closes F-05-1 so the documented migrate command works on a clean checkout, which is what an operator runs from the VPS. Assertion A9 was added mid-session by the operator and is the one that protects the site's central claim: a real Orchard-to-Ironwood migration was classified `Z_TO_T` - value went transparent - on a report whose own `netTransparentInflowZat` was 0n. Two gate rounds, five lenses then five verifiers instructed to refute, 41 findings all with executed probes and all read; the verifiers refuted five including two rated HIGH. THREE OF THE SURVIVING FINDINGS WERE DEFECTS THE FIX ITSELF CREATED: making the fee nullable opened a negative-fee render and a `BigInt(null)` that silently dropped a whole leak record, both unreachable while the column was NOT NULL, and making the migration class reachable made a direction-blind label and a self-contradicting crossing tile live. A9's rule then failed in three more places than the one corrected. 864 tests pass, 1 skipped, up from 704; typecheck 10/10; four static guards, the fourth new (no stale two-pool unions, self-tested in both directions, which caught a flaw in itself on its first run and two more at the gate). A read-only mapping agent wrote to the tree and was reverted, which produced a new CLAUDE.md rule: `git status --porcelain` after every fan-out, before the next commit. LEDGER-06 asks six questions; Q6 reports that the integration suite is not safe against two concurrent vitest runs on one Postgres, proven in both directions and pre-existing; CI is safe as configured because it runs one vitest process per package, which the question's first draft got wrong and which was checked rather than left standing. |
| 2026-08-24 | 07 v6 decoder | [#38](https://github.com/aqua-019/ZCashReveal/pull/38) | shipped (PR opened) | 4 | The v6 transaction format and the Ironwood bundle, decoded, plus the ZIP 318 migration lens - and an abstention path that measures nothing when the shape is one this build does not model. New: `decoder/ironwood.ts` mirroring `orchard.ts`, `decoder/v6.ts` dispatching by version with three refusals (unknown version, an Ironwood bundle below v6, JoinSplits at v5 or above), `packages/zec-types/src/zip318.ts`, `packages/zebra-rpc/src/sprout-field.ts`, migration 004, a synthetic v6 fixture that says in its own README what it is, and a fifth static guard. The unit collision in A4 was real and is settled in one place: the assertion states the ZIP 318 denomination as an exponent over ZEC while `migrations_zip318.denom_k` is an exponent over ZATOSHI with `CHECK (denom_k >= 0)`, so 500 ZEC is (5,2) in one and (5,10) in the other; `Zip318Denomination` carries both and neither is called `k`. Deliverable 2's seam was filled inside `analyze()` rather than at the `index.ts` call site the handoff names, because filling it there would have made the live path work while every other caller - the gateway's projections, every test, HANDOFF-12's replay driver - read an undecoded pool; the context field stays as a three-state override so A8's fail side can withhold it. FOUR GATE ROUNDS, and rounds 2, 3 and 4 each reviewed ONLY the previous round's fix commit. That is what found the six defects the fixes themselves created: round 1's fix swept an `undecoded` row into a subtraction over a closed class set and moved /track's headline shielded share from 7 of 12 to 8 of 13, published a `v6` version cell two columns from its own finding "transaction version 7 is outside the range this decoder models", and rendered "not priced - L = 0" for a transaction nobody decoded; round 2's fix then took the same row out of the numerator and left it in the denominator, moving the same statistic four points the other way, taught the classifier to refuse a migration with a public recipient without teaching the row producer that renders it, and copied a shape test into MIGRATION_S2O that no producible transaction can exercise - Sprout needs `vjoinsplit` which v5 removed, Orchard needs v5 or later, Ironwood needs v6, and NU6.3 made Orchard exit-only in the same upgrade. HANDOFF-06 recorded this pattern once; three sessions in a row makes it a property of the codebase, and it is now the thing the gate is pointed at. Round 1 also found that A3's fail-side probe did not discriminate - a fail-side probe that does not fail, which CLAUDE.md makes a finding in its own right, and the second time this session's rule caught it rather than luck. 953 tests pass and 1 skipped, up from 864; typecheck 10/10; lint 0; five static guards, the fifth new (a corpus line citation that points at a blank line, self-tested in both directions, written because this branch decayed a citation inside the very commit that created it by re-pinning eight of them with arithmetic instead of by reading). LEDGER-07 asks L2 five questions; Q1 reports a HIGH pre-existing defect this handoff makes systematic rather than creates - `RoundTripIndex` emits a round-trip link between two unrelated wallets that move the same amount between pools, and ZIP 318 mandates that migration amounts repeat, so under Ironwood the collision is the design rather than a coincidence. Reproduced in two polarities on committed code, guard written and reverted as out of scope, handed to HANDOFF-08. POST-PR: the first push failed CI and the Vercel preview on one `./format.js` import in the module gate round 4 extracted - correct in the ESM packages, unresolvable in Next - while all five gate commands the contract lists were green, because none of them runs `next build`. `pnpm build` is now in CLAUDE.md's gate list with the reason beside it, and the deployment on the fix is READY (`7wSQeQjj1vA6eNWQtYW6UppoWgLd`), which is the fix confirmed in the real build environment rather than only in the container. The preview host remains unreachable from a session: `curl -I` returns `CONNECT tunnel failed, response 403` before Deployment Protection is reached. |
| 2026-08-29 | 08 analysis toolkit | [#39](https://github.com/aqua-019/ZCashReveal/pull/39) merged mid-gate, then [#40](https://github.com/aqua-019/ZCashReveal/pull/40) | shipped (follow-up PR opened) | 2 (the second incomplete) | TRACKING-MATH sections 1, 3.4 and 4 as five pure estimators plus a sixth the gate forced into existence: `echo.ts` (four rules - exact, fee-tolerant, relative, subset-sum), `clustering.ts`, `labels.ts`, `posterior.ts`, `taint.ts`, and `conservation.ts`. Plus LEDGER-07's eight folds, the WIDE rule on `RoundTripIndex.ingest()`, the 17 round-trip fixtures rebuilt with the transparent side their assertions always meant, the `mixed` row class with its consumer sweep, and the discovery that `packages/zebra-rpc` had NEVER been in CI - found by reading a GREEN log and noticing it ran 1001 tests where the tree had 1036. **PR #39 WAS MERGED TEN MINUTES AFTER ITS LAST PUSH, WHILE FOUR GATE LENSES WERE STILL OUT**, so `main` at `4386e98` carries the toolkit without its fixes and one of those fixes is for a HIGH defect that is live there: `matchEcho` is pure and per-withdrawal, so one deposit can answer several withdrawals - one 100 ZEC deposit and three 100 ZEC withdrawals in one window give three EXACT matches at grade HIGH, 300 ZEC of estimated exits against a pool that held 100, in direct contradiction of section 3.11. **A9 EXISTED TO CATCH EXACTLY THAT AND COULD NOT.** It checked `m.depositAmountZat > balance` per match, and `depositAmountZat` is a sum over a subset of the deposits whose total IS the balance - a tautology no input could falsify. The assertion said sigma and the test never summed; `if (true) return []` at the head of `matchEcho` left all three A9 tests green, and its fail side built `violating` and `audit` as literals in the test file and asserted things about them, so half of 3.11 was unimplemented and the test asserted the test. L2 passed A9 by grepping `numRuns` and reading its describe block while giving the other twelve assertions a mutation - breaking twelve things and reading the thirteenth is how the thirteenth is the one that was wrong. Fourth member of a family this project keeps finding (`expiryheight` casing, `tx.feeZat` at `0n`, the "unknown fee" test that passed `0n`), and every one of them was green: green is the symptom, not the reassurance. Fixed by a module whose API makes the tautology unrepresentable - `enforceConservation` takes the SET - plus a named regression case beside the property. Two new CLAUDE.md rules: a property test is verified by executing the scenario it forbids against the pre-fix code, `numRuns` being a budget not evidence; and every section 5 assertion gets a mutation, an assertion verified by reading being an assertion not verified. All thirteen mutated this session, all thirteen killed tests. Gate round 1's other HIGH was `guessChange` naming the wrong output as change on section 1.4's OWN worked case - 1.3 says the fresh output, 1.4 says the return to the spending address, `detectExchangeShapes` four functions away already said the latter, and because change extends the cluster with weight `p_change` the module was soft-merging a withdrawing customer's address into a named exchange's cluster. A11's fail-side probe was itself a finding for the third time in this project: it built a report that already had both transparent ends, so it passed with the wide rule in place AND reverted, and is now honestly labelled a CONTROL. 1047 tests pass, 1 skipped, up from 953; typecheck 10/10, lint 0, five guards, `pnpm build` green and the Vercel preview DEPLOYED. Round 2 - the fix commit reviewed as its own commit, which is where the last three sessions found the fix's own defects - was still out at write-back and is reported as work, not as a clean round. GATE ROUND 2 - the review of round 1's fix commit as its own commit - RETURNED AFTER THE FIRST WRITE-BACK AND FOUND FOUR MORE HIGHs, every one of them this branch's own work. The conservation sieve written to stop one deposit answering many withdrawals enforced one-to-one on the DEPOSIT SIDE ONLY, so three deposits explaining one withdrawal were all accepted - 300 ZEC of exits published through a transaction that moved 100, the same defect with the two sides swapped, shipped inside its own fix. It also summed `depositAmountZat` where section 3.11 bounds `sum estimated EXITS`, quantities that differ for every inexact match. A third HIGH was a correction repeating the error it corrected: TRACKING-MATH section 3.6 said Ledger is "absent from the corpus" when Ledger is in the SAME section 2.6 the row above cites, in the commit whose own message says that row "was wrong in both halves". A fourth was a sweep that left two sites still stating the superseded claim, which LEDGER-03 Q3 rates HIGH. Also reverted: a roundness tie-break introduced in round 1 to make `isRoundAmount` reachable, which on a batched payment named a THIRD PARTY's address as the spender's change at p_change and merged a stranger into their cluster where the previous code abstained - a branch is not made honest by being made reachable. Eleven mutations that survived round 2 now die, including the gateway coinbase fix and all three taint fixes, none of which had any test. 1061 tests pass, 1 skipped, up from 953 at HANDOFF-07. THE FIRST EXTRAPOLATION THIS SESSION PUBLISHED WAS WRONG AND IS KEPT ON THE RECORD: after round 1 it predicted a further round would find "one or two more, of LOWER reach - a wrong ordinal, a citation off by a section", and round 2 found four HIGHs including two logic defects in new production code. The prediction was wrong in the direction that flatters the branch, about a commit this session had itself written. The pattern is now four sessions old and is a property of the codebase: the dangerous commit is the one that FIXES a defect, because its author has just proved they held a wrong model of what they were editing - and all four of round 2's HIGHs are visible in round 1's own commit message, phrased with more confidence than the code had earned. |
| 2026-08-29 | 08 analysis toolkit (round 3) | [#41](https://github.com/aqua-019/ZCashReveal/pull/41) | shipped (PR opened) | 3 | Gate round 3 reviewed round 2's fix commit and returned AFTER PR #40 merged, so its thirteen findings were live on main and land as a second follow-up off `09b9e9c`. Two HIGH, and both are shapes this session had already written the rule against. The legacy panel published "Nothing was refused" for a window in which two links were: round 2 widened the conservation audit record with a third rejection count and left its only renderer summing two of three, with `countIn 3n` and `countOut 1n` on the same record. `assertNever` could not catch it because the union gained a FIELD, not a member - an exhaustiveness check protects the SET of variants and says nothing about the SHAPE of one. The second HIGH: round 2's lens named the "12 times" error at TWO file:line sites and the session fixed one, in the commit whose message claims the sweep, so the tree carried both numbers for one worked case. And the fix for round 2's finding created a new one a third time - excluding the coinbase vin from `hasTransparentSource` made a previously unreachable branch live, so a ZIP 213 coinbase into two pools classified `migration` with the literal caption "migration" and counted into `summary.migrations`; the test written for that alignment pinned an IMPOSSIBLE input (a positive pool leg on a coinbase) and passed for the wrong reason. 1063 tests pass, 1 skipped. Twenty mutations die where six survived. THE MEASUREMENT: three rounds, three times the fix commit was where the defects were, and the reach did not decay the way LEDGER-07 Q6 predicts - round 3's HIGH is a false sentence rendered to a reader. What decayed was NOVELTY: both HIGHs repeat round 2's two shapes. The session committed those shapes four times after writing the rules against them, which is not a reviewer-attention problem and will not be fixed by another round budget. Section 8 proposes two mechanical guards instead. Both published extrapolations understated severity, and both were about this session's own commits; the honest statement for a fourth round is that no round here has come back empty and none has been run whose fix was not itself found wanting. |
| 2026-08-29 | 08 analysis toolkit (round 4) | [#42](https://github.com/aqua-019/ZCashReveal/pull/42) | shipped (PR opened) | 4 (the fourth mechanical) | L2 ruled that round 3 did NOT clear LEDGER-07 Q6's stopping bar - it returned a false sentence rendered to a reader - so a fourth round was required rather than optional, AND THAT ITS INSTRUMENT CHANGES. Not another review pass: two static guards, run over the whole tree, with their output as the finding list. `check-audit-consumers.mjs` - every `FilterApplication` case block that reads any field of its params must read every field, because `assertNever` protects the SET of variants and says nothing about the SHAPE of one; deliberate omissions record their skipped FIELD LIST, so a variant gaining a field defeats every acknowledgement at once. `check-finding-sites.mjs` - a gate finding naming two or more sites is not closed until every site is, with the site list as data (`absent` for a stale string, `present` for the corrected claim). The trigger was L2's F-41-1: round 3's "12 times" correction fixed the comparison and carried the wrong NUMBER into two of three sites (2,000,000/160,000 = 12.5, not 12), inside the commit that fixed the fourth instance of that same shape - five instances of two shapes, by a session that had written the rule against both. THE GUARDS' FIRST RUNS FOUND SIX THINGS AND THREE WERE IN THE GUARDS: a `matchAll` written backwards that made the scan inspect nothing; a field named in a COMMENT counting as a field read, so a docblock explaining a defect read as evidence of its fix; the finding register reporting the LEDGER as an open site because the ledger quotes findings to record them; the register reporting F-41-1's two sites CLOSED because the sentence wraps across four comment lines; and the discovery that a negative pattern cannot tell an assertion from an explanation of one. In the shipped tree they found exactly one substantive defect: `exitZat` was on every conservation audit record, published by the estimator, and rendered by NOTHING - section 3.11 bounds exits and the panel stated the deposit side only. The claim that a READER saw that is overstated and is corrected here: `parsers.ts` coerces an unknown record into an inert `time_window` and nothing in the legacy app produces a conservation record, so the arm is unreachable there. The record-to-render seam was wrong; no page was. Also recorded: H1's fix from round 3 ships with NO fail-side transcript and cannot have one, because `legacy/dashboard` has no test script; the static guard is the compensating control and is honestly weaker - it proves the fields are read, not that the sentence is right. 1058 tests pass, 1 skipped, matching L2's independent measurement on 4a6a578; section 7's count line had read 1047 for two rounds and PR #41's body said 1063 where the sum is 1058. Seven guards now, and the count is itself in the register. CLAUDE.md gains the escalation: when the same defect SHAPE recurs across three rounds the next round's instrument is a guard, not another review, because a rule the authoring agent has violated more often than honoured is evidence about the instrument. |
| 2026-08-29 | 10 infra | [#43](https://github.com/aqua-019/ZCashReveal/pull/43) | shipped (PR opened) | 3 | Production infrastructure as files, and four spec assumptions the source contradicts. `docker-compose.yml` rewritten: Zebra pinned 4.4.1 -> **6.2.3** (newest 6.2.x, Docker Hub tags API), seven services all with REAL healthchecks, named volumes replacing bind mounts that put a multi-hundred-gigabyte state directory inside the git tree, and an exposure model where exactly ONE port reaches the internet - Zebra's P2P. The gateway publishes nothing and is reached only through the tunnel; the RPC, metrics, health server and Redis are on the compose network and nowhere else, which is what makes `enable_cookie_auth = false` safe. New: `docker-compose.dev.yml`, `.dockerignore`, three service Dockerfiles plus a FOURTH for cloudflared, three healthcheck scripts, `docs/2.0/RUNBOOK-VPS.md`, a path-filtered Playwright workflow, and FOUR static guards - check-compose, check-zebrad-config, check-infra-docs and, after F-43-2, check-ledger-structure (eleven in the tree now, with #42's two). FOUR THINGS THE SPEC ASSUMED THAT ZEBRA'S SOURCE CONTRADICTS, each read at tag v6.2.3 rather than inferred: there is NO address-index config key anywhere in `ZebradConfig` - the address RPCs the gateway depends on are unconditional, declared at `methods.rs` 232/438/459, so the requirement is satisfied by the node rather than by a config line; ZEBRAD EXPOSES NO ZMQ SOCKET, so the indexer's polling fallback is not a degraded mode but the only mode, and a silent one - one WARN line at boot and then polling forever; Zebra 6.x HAS an HTTP health server (`/healthy`, `/ready`) the previous compose file could not have used, and the container check uses `/healthy` because `/ready` is false for the days a first sync takes; and the official cloudflared image is DISTROLESS, so nothing inside it can call cloudflared's own `/ready` - which is why there is a fourth Dockerfile rather than a healthcheck that cannot fail. DELIVERABLE 5, the integration-test isolation LEDGER-06 Q6 asked for: SCHEMA-PER-RUN, chosen over an advisory lock (which buys safety by preventing concurrency, and silently loses the lock if a pool reconnects) and over a database per worker (which needs CREATEDB). Two concurrent runs now pass 60/60 with distinct schemas and nothing left behind; without it the same two runs reproduce LEDGER-06 Q6 in BOTH directions - `expected 4 to be +0` and `expected +0 to be 4` - and the test that fails is the four-pool CONSERVATION assertion. The concurrency probe found a second hazard the schema fix does not cover: `migrations.test.ts` creates three scratch DATABASES with fixed names, and databases live in `pg_database`, which is cluster-wide and has no schema to hide in. A2 and A9 are BLOCKED and reported as such with executed transcripts of the refusal rather than as omissions: `docker build` cannot pull base images (403 from the org egress proxy at `production.cloudfront.docker.com`, all four Dockerfiles, BuildKit's parse is the only verification available), and the mainnet fixture needs a synced node no session can reach - the fourth handoff to carry LEDGER-00 Q4. ONE GATE ROUND, 4 lenses, 52 findings, and its verify phase died at a usage limit after ~7: the lead read the other 45 rather than ship them unread, and labelled that weaker evidence. 22 fixed. The runbook as first written COULD NOT BE EXECUTED - compose interpolates the whole file before deciding what to start, so deferring `TUNNEL_TOKEN` to section 6 made `docker compose config` fail in section 1; `openssl rand -base64 32` produces URL-reserved characters that break `DATABASE_URL` about half the time; and the one alert that matters read a `height` field from `/api/pools`, which answers 503 by design, so it would have fired daily from day one and been muted within a week. FOUR GUARDS WERE BLIND: A5 could not see a secret in a `${VAR:-default}` - and closing that flagged a literal password default THIS SESSION had written to satisfy the earlier blindness; A4 accepted compose's two ways of REMOVING a healthcheck; the service splitter went blind on a trailing comment and misfiled that service's lines onto the previous one; and the zebrad cross-file checks were substring searches over a file whose comments quote the very strings being searched for. THE FIX COMMIT CREATED A DEFECT, four handoffs running now: the replacement alert query read `MAX(height)` from a table whose column is `block_height`, caught by executing it against the live schema rather than by reading it. Gate: 1036 passed / 1 skipped, typecheck 10/10, lint 0, eight static guards, build 7/7, `docker compose config` rc=0 on both files. LEDGER-10 asks four questions; Q1 reports that the pinned node is 6.2.3 while `packages/zebra-rpc` was written against 6.3.0's structs, which is a decision rather than a default. Rebased onto 4ae0796 after #42 merged: ci.yml and package.json reconciled to the UNION of ten guards (the five predating both branches, #42's check-audit-consumers and check-finding-sites, and this handoff's check-compose, check-zebrad-config and check-infra-docs), both LOG rows kept. Re-gated after the merge: 1058 passed / 1 skipped rc=0 (content 67, zebra-rpc 38, web 368, gateway 131, indexer 454), typecheck 10/10, lint clean, content validate OK, pnpm check eleven guards, pnpm build 7/7 with 0 cached. The +22 over the pre-#40 1036 is #40's and #41's, already on this branch before today's merge - #42 added no vitest test at all (fourteen files, no `.test.ts`; what it shipped is two guard scripts, which is why the merge moved the guard count 8 -> 10 and not the test count). This handoff adds no vitest test either; its instrument is four self-testing guards. L2's F-43-1 applied as a RULE rather than a fix (LEDGER-06 Q4): three of check-infra-docs' fourteen topic rows could be closed by a sentence, not one, each fixed to a command shape with a two-polarity transcript on one document. F-43-1's own worked example does not reproduce - "before migrating" does not contain "migrate" - reported rather than silently repaired, which is the rule fold 4 adds to CLAUDE.md. A1 re-executed rather than deferred (docker compose config needs no daemon): rc=0 on both files, and Docker's parser independently confirms the healthchecks, the service_healthy edges and the single 0.0.0.0 binding. A2 still BLOCKED but correctly attributed: dockerd 29.3.1 started, registry answers 401, and it is the layer CDN alone that the egress proxy denies. No container started; docker ps -a and docker images both empty. L2 then verified 56779f8 on a clean worktree and returned F-43-2: THE REBASE MERGE HAD CROSSED TWO HEADINGS OVER THEIR CONTENT IN LEDGER.md - `## HANDOFF-10` spliced under HANDOFF-08's closing sentence so it governed HANDOFF-08's own section 8, and `## HANDOFF-08` left with five lines and no block. Nothing lost, everything filed under the wrong name, in the file HANDOFF-09 reads next by handoff number. Mapping the region before touching it found a THIRD defect L2 did not name: the two handoffs' section 8 blocks shared ONE fence with no separator, so moving the heading alone would have left them concatenated. Answered with guard ELEVEN, `check-ledger-structure.mjs`, whose two rules were each measured against the real damaged file before being written (R1 a heading is preceded by a blank line, R2 a heading governs a fenced block - one finding each, at 2674 and 2668, and zero on the repaired file). L2's own proposed rule was measured and REJECTED: it misses F-43-2 at both sites and fires on three correct blocks. Fold 3 reported as impossible rather than forced - check-finding-sites excludes LEDGER.md by construction and self-tests the exclusion, so registering F-43-2 there would be coverage that checks nothing. And L2's MECHANISM for F-43-2 is wrong in the same shape as F-43-1's: ATX headings interrupt paragraphs, so the spliced heading rendered as an h2 on GitHub all along - executed against the reference commonmark parser. It was never invisible; a heading in the wrong place just looks like one in the right place, which is a stronger argument for the guard than the finding gave. |
| 2026-08-30 | 09 instruments + snapshot | [#44](https://github.com/aqua-019/ZCashReveal/pull/44) | shipped (PR opened) | 1 | The three instruments and the publisher. `turnstile-accounting.ts` (plan 3.1-3.3: U, V, the Orchard drain D and velocity, plus a falsifiable exit-only law with its worked case executed by name), `migration-lens.ts` (plan 3.4 / TRACKING-MATH 3.9), `ironwood-birth.ts` (plan 3.5), all pure, all emitting FilterApplication records - three new union variants registered with `check-audit-consumers.mjs` in the same commit that introduced them, which is LEDGER-10 fold 8 - and a seam test that runs each record through the gateway's flatten-and-parse gate with a negative (the residual narrows nothing, so it emits none) and a fail side (a nested params value IS rejected). `apps/publisher` new: SnapshotV1 with four required fields and every panel nullable, a file sink and a redis sink behind one `MULTI`, a file-backed monthly command counter on a named volume, and a config that calls `assertNotManagedStore` on `REDIS_URL` and `DATABASE_URL` but deliberately not on `SNAPSHOT_REDIS_*`, which is this app's destination. Gateway: the 501 stub replaced by a 200 with max-age=60 and a 503 carrying one of four reasons and `no-store`; the snapshot frame now rides the `{channel, payload}` envelope and arrives before the mempool frame. CI gained a `redis:7` service and a publisher step, so A7's integration half runs rather than skipping - and it ran here too, against a real local Redis. L2's nine folds applied in the first commit, with fold 1's stated REASON corrected against Zebra's source at both tags (the 6.3.0 change is recipient names and specification URLs, not addresses - `subsidy.rs` is byte-identical between v6.2.3 and v6.3.0) and fold 3 reported as rejected by its own guard before being resolved with a proof-based exemption. One gate round, 47 raw findings, 12 refuted, 35 confirmed and all 35 dispositioned with the budget stated in the round's first line. One HIGH: the gateway mounted no snapshot volume, so `GET /api/snapshot` would have answered 503 forever on a correctly-running stack - fixed and then guarded, after the guard's FIRST detector was found unable to catch the defect it was written for (it examined only services that SET `SNAPSHOT_FILE`; the defect was a reader setting nothing). One user-visible MEDIUM that is a spec conflict rather than a code slip: `maxWallets` published TRACKING-MATH 3.9's denomination-run count, which two wallets crossing one denomination in adjacent blocks falsify, and which TIGHTENS as evidence accumulates - 847 identical crossings would have claimed 'at most 1 wallet' over 84,700 ZEC. Now plan 3.4's `Sigma counts`, with the run count beside it as `denominationRuns`, swept across seven sites and recorded SPEC-WAS-AMBIGUOUS for L2. Two probes that could not fail, both found and both repaired with their reason: A11's fail side was reaching `snapshotKeyForHeight`'s throw one function earlier and never running the namespace guard at all, and `check-redis-safety` matched `.scan(` while missing `.scanStream(` - an unbounded enumeration of a store shared with another project's production data. One credential leak: the log redaction's password class excluded `/`, which is in base64's alphabet, so an Upstash token went to the log verbatim. And the FIX COMMIT REVIEW found the round's most interesting defect for the third session running - that redaction fix was quadratic, 16.4 seconds on 200,000 characters in a function that runs on error messages. A10 recorded as narrower than its name: three is the write count, five cross the wire, and whether the meter bills `MULTI`/`EXEC` is unreadable from a container whose proxy blocks upstash.com, so both numbers are pinned and reading the console is now a named operator task. 1137 passed / 1 skipped, typecheck 11/11, lint clean, content validate OK, eleven guards, `pnpm build` 8/8. |
| 2026-08-31 | 09a estimator package | [#45](https://github.com/aqua-019/ZCashReveal/pull/45) | shipped (PR opened) | 3 | The three estimators, plus the three pure leaves they import, moved out of `apps/indexer/src/analysis/` into `packages/zec-instruments` (`@zcashreveal/instruments`), imported by both the indexer and the publisher, and the publisher's composition root now passes `REAL_INSTRUMENTS` instead of `NO_INSTRUMENTS`. Five of six moved sources byte-identical, the sixth differing by one import specifier; four of six moved tests byte-identical, two by a specifier. `activation-heights.ts` moved because a dependency-free package cannot reach back into the app it came from and duplicating a consensus height is worse. The barrel re-exports BY NAME, not `export *`, or it would have gained twenty activation-height constants it never had. Twelfth guard `check-instrument-deps.mjs` in `pnpm check` and CI. **THE MOVE UN-NULLS TWO OF THE FOUR PANELS, NOT FOUR** - `residual` and `migrationHist` are measurements; `drain` and `neffSeries` are still null because `readSnapshotInputs` cannot supply their inputs (a block-time column on `pool_snapshots`, and an Ironwood spend source), which is the input layer rather than the packaging and is now HANDOFF-11's, raised as LEDGER-09a Q1. L2's five folds applied in the first commit; fold 5's third clause reported as ALREADY SATISFIED rather than acted on twice, and fold 2's section-4 reference reported as naming the wrong section. Three gate rounds, five workers. Round 1: one HIGH found by the lead outside the fan-out - CI enumerates a test step per package and had none for the new one, so 98 tests would have left CI green while `pnpm -r test` stayed right; second instance of a shape `ci.yml` itself records for zebra-rpc. Round 2, three HIGHs and two of them created by this handoff: the publisher suite was resolving the package to `dist` so every A1 result was about an artefact; one row `migrations_zip318` PERMITS stopped the publisher publishing any document for its 1,152-block window; and a node reporting `chainSupply` without `valuePools` published '100 per cent of supply is verified' as a measurement. **The guard this handoff exists to deliver was the worst-reviewed artefact in the branch** - eleven holes whose self-test certified all of them, including never exercising `zeromq` at all because its one case routed through a name banned at hop one. Round 3 reviewed round 2's fix commit and both its HIGHs were round 2's own fixes: the vitest alias applied to one of two sites, and the rewritten guard recommitting the class of hole it closed. Two malformed probes reported rather than redone. Stopping stated in three parts with the extrapolation, and clause (b) reported UNSATISFIED for the loose-predicate shape, which reached instance six here - two of the three new ones written by the session holding the fold against them - where clause (b) says build a guard and fold 4 says specify it; LEDGER-09a Q2 asks L2 to settle it. 1218 passed / 2 skipped (was 1204 / 2), typecheck 13/13, lint clean, content validate OK, twelve guards, thirteen registered findings, `pnpm build` 9/9. |
| 2026-08-31 | 09b snapshot inputs | [#46](https://github.com/aqua-019/ZCashReveal/pull/46) | shipped (PR opened) | 3 (each reviewing the previous fix commit) | The two missing input SOURCES HANDOFF-09a's principal finding exposed. Migration 005 adds `blocks (height, time_s, hash)` - a TABLE, not a column on `pool_snapshots`, because a block has one time and a column stores that one consensus number four times per height, and because `PoolStateSnapshot` is a shared type with no time field so the column shape forces widening it or breaking the uniform writer signature - plus `pool_nullifiers.anchor_root`, the missing (nullifier -> anchor) edge. `writeBlock` and `writePoolSnapshot` are the first writer `pool_snapshots` has ever had. `readSnapshotInputs` reads both, so `drain` and `neffSeries` are measurements on the production path and all four panels are non-null, asserted by VALUE against a real Postgres 16. **Two premises in the commissioned scope were false**: `pool_nullifiers` has admitted ironwood since 003 widened its CHECK by name (proven against `pg_constraint`, with `'tachyon'` rejected so the constraint is shown live), which changed the deliverable from a `candidate_count` column to the anchor edge; and `pool_snapshots` had no writer at all, so there is nothing to backfill anywhere, ever. A4 is the sharp assertion: block time gives -1,000 ZEC/h and the write clock gives -172,043,100. **Three gate rounds, budgets in every first line (28/24 and 16/14, then 34/24, then 57/44 with ten mutations, nine killing their target and one surviving).** Round 1: the publisher suite was TRUNCATING the shared database, an empty join published `neffSeries` as a measured zero, `readSnapshotInputs` had no try/catch under a docblock that said in capitals that it did, and the three production queries had ZERO execution coverage - breaking all three left the suite green. Round 2: the fix commit had reintroduced the mixed-chain row one table over, and I had edited migration 005's bytes after application - my own rule, one commit after writing it for 003. Round 3: the surviving mutation (`height > 0` rejects genesis and passes 444 tests), a correction landing in one file while the branch's own two restatements stood, and a count in two commit messages that did not reproduce. **The gate has NOT converged and says so**: round 3 returned findings a user could see and its fix commit is unreviewed. Folds 1-6 applied; fold 5 was already satisfied and is reported rather than applied twice; fold 1's stated verification does not discriminate and is corrected in place. 1220 -> 1266 tests, measured per package. |
| 2026-08-31 | 09b snapshot inputs (round 4) | [#46](https://github.com/aqua-019/ZCashReveal/pull/46) | shipped (PR open, round 4 pushed) | 4 | L2 gated PR #46 DO NOT MERGE and reviewed `0e2df0c` as its own commit because nobody had - the review section 7 itself named as missing. **F-46-1 (MEDIUM): round 3's fix corrected the RENDERING layer and left the LOG layer stating the falsehood it removed.** The pre-birth branch returns a measurement - `spends: []` over a real window - and still called `fault("neffSeries", ...)`, whose one production wiring logs at ERROR "an input query failed". Both halves false, on ~3.4 million blocks of an initial sync, demonstrated with a query that throws if called: it never is. Round 3 had PINNED ITS OWN DEFECT as correct behaviour, which is why it survived that round. Fixed as no report at all rather than a quieter channel, argued on four grounds. **And the round-4 fix comment repeated the shape it was fixing** - claiming the condition is readable from a window that is not published - caught by measuring the document rather than re-reading the sentence, with the test carrying the same confusion (asserting on the inputs where the claim is about the document). L2 also records ITS OWN error: the section 1 premise about `pool_nullifiers` was false because it enumerated every CREATE TABLE and migration 003 widened the constraint with an ALTER - yielding the rule that an exhaustive claim is only exhaustive over the thing it enumerates, and the thing to enumerate is the OBJECT rather than a source that constructs it. Four rulings into CLAUDE.md before any work: that rule, every scripted replacement asserting its pattern matched, counting instances against the ORIGIN rather than the face so a shipped guard does not reset the count, and clause (ii) bounded so the fix-commit regress terminates where a fix can no longer carry a behavioural defect. Q4's form-A guard deferred to HANDOFF-12 fold 1 with its three hits named. 1267 tests measured per package, twelve guards, typecheck, lint, build green. |
