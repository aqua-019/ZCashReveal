# LOG — ZECReveal 2.0 revolutions

| date | handoff | PR | status | gate rounds | notes |
|---|---|---|---|---|---|
| 2026-08-22 | — | — | handoffs 00–13 written by L2 | — | 00 open, rest queued |
| 2026-08-22 | 00 housekeeping | [#31](https://github.com/aqua-019/ZCashReveal/pull/31) | shipped (PR opened, CI green) | 0 | docs to docs/2.0, handoffs to root, CLAUDE.md 2.0, CI runs 178 tests on postgres:16 (run 32603472860 SUCCESS: indexer 170 passed/1 skipped, gateway 7 passed), eslint flat config, dashboard parked under legacy/. A5 corrected: 1 test is fixture-gated, not Postgres-gated. Delivered on branch claude/aqua-stack-v4-1-handoff-818gb3, not feat/v2-00-housekeeping. |
| 2026-08-22 | 00 housekeeping | [#31](https://github.com/aqua-019/ZCashReveal/pull/31) | closed (merged 0031d7c) | 0 | L2 re-executed all nine assertions on a clean worktree of 514ae4c: all hold, transcripts accurate. CI green on the PR head (run 32603703571). Vercel red check confirmed pre-existing on main. Ledger questions 1-4 answered; folds applied to 01, 05, 06, 10. HANDOFF-01 opened. |
| 2026-08-23 | 01 web scaffold | [#32](https://github.com/aqua-019/ZCashReveal/pull/32) | shipped (PR opened) | 2 | apps/web scaffolded: Next.js 15.5.23, React 19, Tailwind v4, tokens harvested from the mockup, 19 primitives, 3 ambience components, 9 routes + OG + dev gallery. 96 vitest + 19 Playwright tests. All eight section 5 assertions carry two-polarity transcripts. Lighthouse on /beware: performance 99, accessibility 100. Two gate rounds: the dev-surface gate failed open, the splash pool bar collapsed to 2px slivers, .fair was applied and undefined, and two ink tokens failed WCAG AA on body text. LEDGER-01 Q1 asks L2 to rule on the ink-token divergence from the mockup. |
| 2026-08-23 | 02 content package | [#33](https://github.com/aqua-019/ZCashReveal/pull/33) | shipped (PR opened) | 4 | packages/content: zod schemas, typed loaders, a validator wired into CI, and ten seeds transcribed from the dossiers. beware 14, contradictions 16, timeline 124, network 36 entities / 41 edges, phrases 19, labels 8, cases 3, unverified 32, sources 328 with 186 cited. sources.json is generated from the corpus and every url is provenance-checked against docs/2.0/research; citations are written as urls and resolved to ids by script, so a citation from outside the corpus cannot survive. All seven section 5 assertions carry two-polarity transcripts. 58 vitest tests. Four gate rounds: the timeline was missing the whole promotion network and carried a corrected ticker in a title, two network exposures outran the corpus, and a Form 144 was attributed to the wrong Silbert entity. LEDGER-02 Q6 asks L2 to rule on the fourth round, which overruns the Loop 4 cap deliberately. A second L2 RESOLUTION arrived mid-session: the root vercel.json is deleted (it applied to every project in the repo and broke zecreveal's first build with NEXT_OUTPUT_DIR_MISSING), its settings recorded in DEPLOY-2.0.md for the operator to move into z-cash-reveal-dashboard2, and a new assertion A8 with a CI check stops it coming back. The deployment on the deleting commit then failed identically: the same settings are stored on the zecreveal project too, so apps/web/vercel.json now pins framework, install, build and output, which take precedence over stored project settings. GitHub Actions green throughout (run 32611476921), and the deployment on the pinning commit succeeded: dpl_CrmijWHtimk842w65Tkux7vibZg1 READY in 19 s, twelve routes prerendered. |
| 2026-08-23 | 03 record pages | [#34](https://github.com/aqua-019/ZCashReveal/pull/34) | shipped (PR opened) | 4 | The Record, rendered from packages/content: eight surfaces, 24 new components, the citation apparatus on every claim (id, confidence, absolute canonical URL, sources), four inline-SVG charts each with a table twin, /beware.xml as RSS 2.0 and ten prerendered JSON exports. Fonts vendored to next/font/local, so a build inside an empty network namespace reaches Generating static pages - the flake that blocked HANDOFF-10's Playwright job is gone. pnpm -r test made self-sufficient (L2 finding F-02-1), and the first fix for it raced two concurrent tsc -b, which is why the suites now resolve the types package to source. Ten of eleven section 5 assertions hold with two-polarity transcripts; A5 is accessibility 100 (floor 95) and performance 94 (floor 95), taken from 89 by four real reductions and then reported rather than shaved - LEDGER-03 Q1 asks L2 to rule on a floor set against a two-row placeholder. Four gate rounds. Two reviewers returned 37 findings: the Silbert Form 144 was still misattributed in contradictions.json and network.json after HANDOFF-02 corrected only timeline.json, the Grayscale custodian was crossed with Cypherpunk's, Zooko Wilcox's compensation was stated two ways on two pages, a <details> inside a <p> broke hydration twice in one session, six --ink-faint text uses failed AA, and the block-arrival ceremony was running on every Record page while A6 passed for the wrong reason. Six defects were in the session's own tests. 68 Playwright, 337 unit tests, all workspace gates green; Vercel preview READY on the branch head, which is the first evidence the pinned build command handles the new workspace dependency. |
| 2026-08-23 | 04 tracking ui | [#35](https://github.com/aqua-019/ZCashReveal/pull/35) | shipped (PR opened) | 1 | The Tracking suite in fixture mode: seven routes (/track with search and a live mempool panel, /address, /tx, /block, /pools, /reveal, /track/flows), the nine-member ZecApi with a fixture implementation and an unused HttpApi against the same interface, every view DTO as a zod schema in packages/zec-types, and a fixture corpus whose every aggregate is derived from its own components - which surfaced four self-contradictions in the mockup, each corrected with a stated reason. Deliverable 1 first, per LEDGER-03 fold 6: three preformatted treatments, two compact-cell registers and seven card insets collapsed, held by A9 at maxDiffPixels 0 on /beware and /flows. Deliverable 9: Unverified.surface on the content schema, permalink() reading it, and apps/web's split module deleted. /track's client bundle taken from 217 kB to 115 kB by two empirical findings - a barrel import and zod on the streaming path. A11 and A12 were added mid-session by the operator. A11 measures the viewing-key promise from outside the browser and is what found that React serialises a controlled input's value into the DOM ATTRIBUTE; both key-capable fields are uncontrolled and the page now ships a CSP with connect-src 'self' and form-action 'none'. A12 is the deployed preview: dpl_J5ryna9fz6mh2TUgZmPTXWxSLNVF READY in 31 s, against dpl_Aca2Y5zfqgi2H2UFX4BbhTEgsEf5 ERROR on the previous commit with "Module not found: Can't resolve '@zcashreveal/types'" - vercel.json now builds through turbo and check-vercel-config.mjs asserts that form. One gate round, four reviewers, all FAIL, 36 findings: two HIGH claims the site's own arithmetic contradicted (a hardcoded 'broad' claim level where claimLevelFor says aggregate_only, and an EstimateCell rendering a claim chip with no assumptions), a ZIP 1015 coinbase split the corpus contradicts by a factor of 3.3, three absolute non-existence claims the Record itself refuses to make, 'Powers of Tau' attributed to Sprout, a tolerance factor off by twenty, the Mode B fog painting text at 1.18:1, and gold on five surfaces none of its four jobs licenses. Lighthouse after the gate: accessibility 100 on all seven routes, performance 95-97 except /address at 94, which is the audit trail the gate required. 127 Playwright and 548 unit tests green. LEDGER-04 asks L2 six questions; Q3 reports a second wall in front of the deployed measurement - this container's egress proxy refuses the preview host, independently of Deployment Protection. |
| 2026-08-23 | 05 gateway api | [#36](https://github.com/aqua-019/ZCashReveal/pull/36) | shipped (PR opened) | 3 | The read API v2 on the Fastify gateway: twelve route modules at BOTH `/api` and `/v2` (the handoff says one, HANDOFF-04's only written client sends the other, and mounting one would have broken the other at cutover - LEDGER-05 Q1 asks which survives), every response validated against its zod DTO before it is serialised, labels and cases served from packages/content with the labeller and precedence rank travelling with every label, a Postgres cache-aside on migration 003a, `@fastify/rate-limit` per IP, a WS cap closing with 1013, and `packages/zebra-rpc` - a typed, retrying, zod-validated client written against Zebra 6.3.0's STRUCTS rather than its doc comments, four of which contradict its own serialisation code. `apps/indexer/src/zebrad-rpc.ts` deleted. `/api/pools` answers 503 naming the four blocks HANDOFF-06 to -09 own rather than fabricating them; `/api/snapshot` answers 501. Assertion A9 was added mid-session by the operator after the session's own 404 finding: a pino serialiser that logs the method, the request id and the PATH, drops the query string entirely and redacts anything viewing-key-shaped in what remains, asserted over a captured log stream. The other operator message found that the wallet fingerprints were INERT, not degraded - `RpcTransaction` declared `expiryHeight` and every node sends `expiryheight`, so `expiryDelta` was null for every transaction ever processed and the existing tests passed vacuously over hand-written fixtures. A real-shaped fixture and a casing lint now exist; the fix revives YWALLET and ZECWALLET_LITE, and NIGHTHAWK and ZCASHD_RUST stay blind because the fee they gate on is also absent from the wire. Three gate rounds: round 1 (lead, 18 findings) inverted pool-delta sign, a gross figure published as net, ZIP 317 computed three ways wrongly, `summary.bytes` hardcoded to a zero the site renders as "0.0 kB", a 404 handler echoing the query string, and a planted ten-fold error in the zatoshi conversion that the suite did not catch. Round 2 (Workflow, four lenses, all FAIL, 39 findings) found the rate limiter was not per-reader behind a proxy at all, Sprout omitted from the value balance, Orchard actions double-counted, a conventional-fee count built from the wallet guess rather than the fee, and an IEEE-754 justification the session itself had published that is arithmetically false - swept across all six restatements including HANDOFF-04's fixture. Round 3 read the 19 HIGH/MID findings round 2 returned UNVERIFIED against an undeclared cap: two were still live and both were real. 704 unit tests green (gateway 90, zebra-rpc 23, content 67, web 346, indexer 178/1 skipped), typecheck 10/10, lint 0 errors. Postgres 16 was started so the gated integration test would run rather than be reported as a skip. LEDGER-05 asks L2 six questions; Q3 reports that `docs/2.0/TRACKING-MATH.md` §3.5 and /method state a ZIP 317 formula that diverges from the protocol exactly on the ZIP 271 lockbox, the one address the project exists to track. A THIRD operator message arrived after PR #36 opened - the L2 NOTE that the Vercel-managed Redis is connected AND is SHARED with an unrelated production project - and was applied to the same branch under its own instruction. It corrected the repository as well as constraining it: the five variable names Vercel actually injects under the `SNAPSHOT_REDIS` prefix are not the three this tree stated in thirteen places, and HANDOFF-11's SnapshotStore keyed its whole resolution order on two names that are injected by nothing, so code built to that spec would have read undefined, fallen through to the fixture and rendered a stale site reporting no fault. New: `docs/2.0/SNAPSHOT.md` (the topology, the `zecreveal:` namespace rule, the forbidden commands, the shared budget, the operator's exit condition), `scripts/check-redis-safety.mjs` (20 self-tested detectors, in CI and `pnpm check`), and `packages/zec-types/src/redis-topology.ts` (both prefixes as constants plus `assertNotManagedStore`, which the gateway and the indexer now use to REFUSE TO START if a Redis URL they would dial is the managed store - by host and by exact value match). An adversarial sweep of 37 agents over the whole tree found what the first pass missed: `UNLINK` defeated the no-DEL-by-pattern rule by one word, `redis-cli --scan/--bigkeys/--hotkeys/--memkeys/--rdb` bypassed both enumeration detectors and are the form a runbook uses, every rule was key-scoped so MONITOR/RANDOMKEY/DBSIZE/INFO keyspace fell outside all of them (now rule 7, because sharing a database is a confidentiality problem in both directions), a self-test fixture passed by matching its own explanatory comment rather than the call it certified, the whole command budget counted only writes while `apps/web`'s reads are the unbounded half, and HANDOFF-11 §3 specified `@upstash/ratelimit` on the shared store's read-write credentials - a per-request writer, outside the namespace, in another company's database - which is struck and pointed at the gateway's own limiter. LEDGER-05's addendum asks four more questions, including who owns the read side of the budget and whether Preview should stay connected. |
| 2026-08-23 | 06 four pools | [#37](https://github.com/aqua-019/ZCashReveal/pull/37) | shipped (PR opened) | 2 | The pool model widened from two to four across types, state, persistence and analysis. `ShieldedPool` is sprout|sapling|orchard|ironwood and stays the single source of truth: `Pool` aliases it, `poolNameSchema` mirrors it, migration 003 mirrors it in five CHECK constraints, and views.ts now carries a compile-time assertion that the type and the zod enum are mutually assignable - checked in BOTH directions, because one alone passes while one union is a subset of the other, which is exactly the state this repository was in. The widening produced ZERO compile errors in apps/, which is the finding rather than the relief: the display layer already spoke four pools, the domain layer did not, and nothing connected them. Sprout was invisible - its movement is a JoinSplit sum, not a valueBalance field, so a Sprout transaction had a boundary of exactly zero and was classified transparent throughout; `vjoinsplit` is now a declared field rather than four `as unknown as` reads. Orchard is exit-only from NU6.3 and `ValuePool.apply` throws `ExitOnlyViolation` on value entering it, network-aware, activation-inclusive. THE FEE NOW EXISTS: no node sends one, so `BigInt(tx.feeZat ?? 0)` was `0n` for every transaction this project ever analysed, and since ZIP 317's conventional fee has a floor of 10,000 zatoshi, two wallet signatures were not degraded but INERT with every test passing vacuously. `computeFeeZat` sums the spent outputs through an injected resolver and returns null rather than a partial sum; `PrevOutCache` supplies it on the live path, bounded by retained outputs after the gate measured 420 MB at the old entry-count bound. ZIP 317 had FOUR implementations that disagreed; one is canonical now, taken from Zebra, and TRACKING-MATH 3.5 and /method state the byte rule with the count form beside it and the ZIP 271 lockbox worked out (two 2-of-3 P2SH inputs: L=4 and 20,000 zat against the count form's L=2 and 10,000). Migration 003 widens five CHECKs, adds pool_snapshots and migrations_zip318, adds a Sprout balance column, drops fee_zat's NOT NULL DEFAULT 0 AND rewrites the rows already carrying that false zero; the runner is transactional per migration; `premigrate` closes F-05-1 so the documented migrate command works on a clean checkout, which is what an operator runs from the VPS. Assertion A9 was added mid-session by the operator and is the one that protects the site's central claim: a real Orchard-to-Ironwood migration was classified `Z_TO_T` - value went transparent - on a report whose own `netTransparentInflowZat` was 0n. Two gate rounds, five lenses then five verifiers instructed to refute, 41 findings all with executed probes and all read; the verifiers refuted five including two rated HIGH. THREE OF THE SURVIVING FINDINGS WERE DEFECTS THE FIX ITSELF CREATED: making the fee nullable opened a negative-fee render and a `BigInt(null)` that silently dropped a whole leak record, both unreachable while the column was NOT NULL, and making the migration class reachable made a direction-blind label and a self-contradicting crossing tile live. A9's rule then failed in three more places than the one corrected. 864 tests pass, 1 skipped, up from 704; typecheck 10/10; four static guards, the fourth new (no stale two-pool unions, self-tested in both directions, which caught a flaw in itself on its first run and two more at the gate). A read-only mapping agent wrote to the tree and was reverted, which produced a new CLAUDE.md rule: `git status --porcelain` after every fan-out, before the next commit. LEDGER-06 asks six questions; Q6 reports that the integration suite is not safe against two concurrent vitest runs on one Postgres, proven in both directions and pre-existing; CI is safe as configured because it runs one vitest process per package, which the question's first draft got wrong and which was checked rather than left standing. |
| 2026-08-24 | 07 v6 decoder | [#38](https://github.com/aqua-019/ZCashReveal/pull/38) | shipped (PR opened) | 4 | The v6 transaction format and the Ironwood bundle, decoded, plus the ZIP 318 migration lens - and an abstention path that measures nothing when the shape is one this build does not model. New: `decoder/ironwood.ts` mirroring `orchard.ts`, `decoder/v6.ts` dispatching by version with three refusals (unknown version, an Ironwood bundle below v6, JoinSplits at v5 or above), `packages/zec-types/src/zip318.ts`, `packages/zebra-rpc/src/sprout-field.ts`, migration 004, a synthetic v6 fixture that says in its own README what it is, and a fifth static guard. The unit collision in A4 was real and is settled in one place: the assertion states the ZIP 318 denomination as an exponent over ZEC while `migrations_zip318.denom_k` is an exponent over ZATOSHI with `CHECK (denom_k >= 0)`, so 500 ZEC is (5,2) in one and (5,10) in the other; `Zip318Denomination` carries both and neither is called `k`. Deliverable 2's seam was filled inside `analyze()` rather than at the `index.ts` call site the handoff names, because filling it there would have made the live path work while every other caller - the gateway's projections, every test, HANDOFF-12's replay driver - read an undecoded pool; the context field stays as a three-state override so A8's fail side can withhold it. FOUR GATE ROUNDS, and rounds 2, 3 and 4 each reviewed ONLY the previous round's fix commit. That is what found the six defects the fixes themselves created: round 1's fix swept an `undecoded` row into a subtraction over a closed class set and moved /track's headline shielded share from 7 of 12 to 8 of 13, published a `v6` version cell two columns from its own finding "transaction version 7 is outside the range this decoder models", and rendered "not priced - L = 0" for a transaction nobody decoded; round 2's fix then took the same row out of the numerator and left it in the denominator, moving the same statistic four points the other way, taught the classifier to refuse a migration with a public recipient without teaching the row producer that renders it, and copied a shape test into MIGRATION_S2O that no producible transaction can exercise - Sprout needs `vjoinsplit` which v5 removed, Orchard needs v5 or later, Ironwood needs v6, and NU6.3 made Orchard exit-only in the same upgrade. HANDOFF-06 recorded this pattern once; three sessions in a row makes it a property of the codebase, and it is now the thing the gate is pointed at. Round 1 also found that A3's fail-side probe did not discriminate - a fail-side probe that does not fail, which CLAUDE.md makes a finding in its own right, and the second time this session's rule caught it rather than luck. 953 tests pass and 1 skipped, up from 864; typecheck 10/10; lint 0; five static guards, the fifth new (a corpus line citation that points at a blank line, self-tested in both directions, written because this branch decayed a citation inside the very commit that created it by re-pinning eight of them with arithmetic instead of by reading). LEDGER-07 asks L2 five questions; Q1 reports a HIGH pre-existing defect this handoff makes systematic rather than creates - `RoundTripIndex` emits a round-trip link between two unrelated wallets that move the same amount between pools, and ZIP 318 mandates that migration amounts repeat, so under Ironwood the collision is the design rather than a coincidence. Reproduced in two polarities on committed code, guard written and reverted as out of scope, handed to HANDOFF-08. POST-PR: the first push failed CI and the Vercel preview on one `./format.js` import in the module gate round 4 extracted - correct in the ESM packages, unresolvable in Next - while all five gate commands the contract lists were green, because none of them runs `next build`. `pnpm build` is now in CLAUDE.md's gate list with the reason beside it, and the deployment on the fix is READY (`7wSQeQjj1vA6eNWQtYW6UppoWgLd`), which is the fix confirmed in the real build environment rather than only in the container. The preview host remains unreachable from a session: `curl -I` returns `CONNECT tunnel failed, response 403` before Deployment Protection is reached. |
| 2026-08-29 | 08 analysis toolkit | [#39](https://github.com/aqua-019/ZCashReveal/pull/39) merged mid-gate, then [#40](https://github.com/aqua-019/ZCashReveal/pull/40) | shipped (follow-up PR opened) | 2 (the second incomplete) | TRACKING-MATH sections 1, 3.4 and 4 as five pure estimators plus a sixth the gate forced into existence: `echo.ts` (four rules - exact, fee-tolerant, relative, subset-sum), `clustering.ts`, `labels.ts`, `posterior.ts`, `taint.ts`, and `conservation.ts`. Plus LEDGER-07's eight folds, the WIDE rule on `RoundTripIndex.ingest()`, the 17 round-trip fixtures rebuilt with the transparent side their assertions always meant, the `mixed` row class with its consumer sweep, and the discovery that `packages/zebra-rpc` had NEVER been in CI - found by reading a GREEN log and noticing it ran 1001 tests where the tree had 1036. **PR #39 WAS MERGED TEN MINUTES AFTER ITS LAST PUSH, WHILE FOUR GATE LENSES WERE STILL OUT**, so `main` at `4386e98` carries the toolkit without its fixes and one of those fixes is for a HIGH defect that is live there: `matchEcho` is pure and per-withdrawal, so one deposit can answer several withdrawals - one 100 ZEC deposit and three 100 ZEC withdrawals in one window give three EXACT matches at grade HIGH, 300 ZEC of estimated exits against a pool that held 100, in direct contradiction of section 3.11. **A9 EXISTED TO CATCH EXACTLY THAT AND COULD NOT.** It checked `m.depositAmountZat > balance` per match, and `depositAmountZat` is a sum over a subset of the deposits whose total IS the balance - a tautology no input could falsify. The assertion said sigma and the test never summed; `if (true) return []` at the head of `matchEcho` left all three A9 tests green, and its fail side built `violating` and `audit` as literals in the test file and asserted things about them, so half of 3.11 was unimplemented and the test asserted the test. L2 passed A9 by grepping `numRuns` and reading its describe block while giving the other twelve assertions a mutation - breaking twelve things and reading the thirteenth is how the thirteenth is the one that was wrong. Fourth member of a family this project keeps finding (`expiryheight` casing, `tx.feeZat` at `0n`, the "unknown fee" test that passed `0n`), and every one of them was green: green is the symptom, not the reassurance. Fixed by a module whose API makes the tautology unrepresentable - `enforceConservation` takes the SET - plus a named regression case beside the property. Two new CLAUDE.md rules: a property test is verified by executing the scenario it forbids against the pre-fix code, `numRuns` being a budget not evidence; and every section 5 assertion gets a mutation, an assertion verified by reading being an assertion not verified. All thirteen mutated this session, all thirteen killed tests. Gate round 1's other HIGH was `guessChange` naming the wrong output as change on section 1.4's OWN worked case - 1.3 says the fresh output, 1.4 says the return to the spending address, `detectExchangeShapes` four functions away already said the latter, and because change extends the cluster with weight `p_change` the module was soft-merging a withdrawing customer's address into a named exchange's cluster. A11's fail-side probe was itself a finding for the third time in this project: it built a report that already had both transparent ends, so it passed with the wide rule in place AND reverted, and is now honestly labelled a CONTROL. 1047 tests pass, 1 skipped, up from 953; typecheck 10/10, lint 0, five guards, `pnpm build` green and the Vercel preview DEPLOYED. Round 2 - the fix commit reviewed as its own commit, which is where the last three sessions found the fix's own defects - was still out at write-back and is reported as work, not as a clean round. GATE ROUND 2 - the review of round 1's fix commit as its own commit - RETURNED AFTER THE FIRST WRITE-BACK AND FOUND FOUR MORE HIGHs, every one of them this branch's own work. The conservation sieve written to stop one deposit answering many withdrawals enforced one-to-one on the DEPOSIT SIDE ONLY, so three deposits explaining one withdrawal were all accepted - 300 ZEC of exits published through a transaction that moved 100, the same defect with the two sides swapped, shipped inside its own fix. It also summed `depositAmountZat` where section 3.11 bounds `sum estimated EXITS`, quantities that differ for every inexact match. A third HIGH was a correction repeating the error it corrected: TRACKING-MATH section 3.6 said Ledger is "absent from the corpus" when Ledger is in the SAME section 2.6 the row above cites, in the commit whose own message says that row "was wrong in both halves". A fourth was a sweep that left two sites still stating the superseded claim, which LEDGER-03 Q3 rates HIGH. Also reverted: a roundness tie-break introduced in round 1 to make `isRoundAmount` reachable, which on a batched payment named a THIRD PARTY's address as the spender's change at p_change and merged a stranger into their cluster where the previous code abstained - a branch is not made honest by being made reachable. Eleven mutations that survived round 2 now die, including the gateway coinbase fix and all three taint fixes, none of which had any test. 1061 tests pass, 1 skipped, up from 953 at HANDOFF-07. THE FIRST EXTRAPOLATION THIS SESSION PUBLISHED WAS WRONG AND IS KEPT ON THE RECORD: after round 1 it predicted a further round would find "one or two more, of LOWER reach - a wrong ordinal, a citation off by a section", and round 2 found four HIGHs including two logic defects in new production code. The prediction was wrong in the direction that flatters the branch, about a commit this session had itself written. The pattern is now four sessions old and is a property of the codebase: the dangerous commit is the one that FIXES a defect, because its author has just proved they held a wrong model of what they were editing - and all four of round 2's HIGHs are visible in round 1's own commit message, phrased with more confidence than the code had earned. |
| 2026-08-29 | 08 analysis toolkit (round 3) | [#41](https://github.com/aqua-019/ZCashReveal/pull/41) | shipped (PR opened) | 3 | Gate round 3 reviewed round 2's fix commit and returned AFTER PR #40 merged, so its thirteen findings were live on main and land as a second follow-up off `09b9e9c`. Two HIGH, and both are shapes this session had already written the rule against. The legacy panel published "Nothing was refused" for a window in which two links were: round 2 widened the conservation audit record with a third rejection count and left its only renderer summing two of three, with `countIn 3n` and `countOut 1n` on the same record. `assertNever` could not catch it because the union gained a FIELD, not a member - an exhaustiveness check protects the SET of variants and says nothing about the SHAPE of one. The second HIGH: round 2's lens named the "12 times" error at TWO file:line sites and the session fixed one, in the commit whose message claims the sweep, so the tree carried both numbers for one worked case. And the fix for round 2's finding created a new one a third time - excluding the coinbase vin from `hasTransparentSource` made a previously unreachable branch live, so a ZIP 213 coinbase into two pools classified `migration` with the literal caption "migration" and counted into `summary.migrations`; the test written for that alignment pinned an IMPOSSIBLE input (a positive pool leg on a coinbase) and passed for the wrong reason. 1063 tests pass, 1 skipped. Twenty mutations die where six survived. THE MEASUREMENT: three rounds, three times the fix commit was where the defects were, and the reach did not decay the way LEDGER-07 Q6 predicts - round 3's HIGH is a false sentence rendered to a reader. What decayed was NOVELTY: both HIGHs repeat round 2's two shapes. The session committed those shapes four times after writing the rules against them, which is not a reviewer-attention problem and will not be fixed by another round budget. Section 8 proposes two mechanical guards instead. Both published extrapolations understated severity, and both were about this session's own commits; the honest statement for a fourth round is that no round here has come back empty and none has been run whose fix was not itself found wanting. |
| 2026-08-29 | 08 analysis toolkit (round 4) | [#42](https://github.com/aqua-019/ZCashReveal/pull/42) | shipped (PR opened) | 4 (the fourth mechanical) | L2 ruled that round 3 did NOT clear LEDGER-07 Q6's stopping bar - it returned a false sentence rendered to a reader - so a fourth round was required rather than optional, AND THAT ITS INSTRUMENT CHANGES. Not another review pass: two static guards, run over the whole tree, with their output as the finding list. `check-audit-consumers.mjs` - every `FilterApplication` case block that reads any field of its params must read every field, because `assertNever` protects the SET of variants and says nothing about the SHAPE of one; deliberate omissions record their skipped FIELD LIST, so a variant gaining a field defeats every acknowledgement at once. `check-finding-sites.mjs` - a gate finding naming two or more sites is not closed until every site is, with the site list as data (`absent` for a stale string, `present` for the corrected claim). The trigger was L2's F-41-1: round 3's "12 times" correction fixed the comparison and carried the wrong NUMBER into two of three sites (2,000,000/160,000 = 12.5, not 12), inside the commit that fixed the fourth instance of that same shape - five instances of two shapes, by a session that had written the rule against both. THE GUARDS' FIRST RUNS FOUND SIX THINGS AND THREE WERE IN THE GUARDS: a `matchAll` written backwards that made the scan inspect nothing; a field named in a COMMENT counting as a field read, so a docblock explaining a defect read as evidence of its fix; the finding register reporting the LEDGER as an open site because the ledger quotes findings to record them; the register reporting F-41-1's two sites CLOSED because the sentence wraps across four comment lines; and the discovery that a negative pattern cannot tell an assertion from an explanation of one. In the shipped tree they found exactly one substantive defect: `exitZat` was on every conservation audit record, published by the estimator, and rendered by NOTHING - section 3.11 bounds exits and the panel stated the deposit side only. The claim that a READER saw that is overstated and is corrected here: `parsers.ts` coerces an unknown record into an inert `time_window` and nothing in the legacy app produces a conservation record, so the arm is unreachable there. The record-to-render seam was wrong; no page was. Also recorded: H1's fix from round 3 ships with NO fail-side transcript and cannot have one, because `legacy/dashboard` has no test script; the static guard is the compensating control and is honestly weaker - it proves the fields are read, not that the sentence is right. 1058 tests pass, 1 skipped, matching L2's independent measurement on 4a6a578; section 7's count line had read 1047 for two rounds and PR #41's body said 1063 where the sum is 1058. Seven guards now, and the count is itself in the register. CLAUDE.md gains the escalation: when the same defect SHAPE recurs across three rounds the next round's instrument is a guard, not another review, because a rule the authoring agent has violated more often than honoured is evidence about the instrument. |
| 2026-08-29 | 10 infra | [#43](https://github.com/aqua-019/ZCashReveal/pull/43) | shipped (PR opened) | 3 | Production infrastructure as files, and four spec assumptions the source contradicts. `docker-compose.yml` rewritten: Zebra pinned 4.4.1 -> **6.2.3** (newest 6.2.x, Docker Hub tags API), seven services all with REAL healthchecks, named volumes replacing bind mounts that put a multi-hundred-gigabyte state directory inside the git tree, and an exposure model where exactly ONE port reaches the internet - Zebra's P2P. The gateway publishes nothing and is reached only through the tunnel; the RPC, metrics, health server and Redis are on the compose network and nowhere else, which is what makes `enable_cookie_auth = false` safe. New: `docker-compose.dev.yml`, `.dockerignore`, three service Dockerfiles plus a FOURTH for cloudflared, three healthcheck scripts, `docs/2.0/RUNBOOK-VPS.md`, a path-filtered Playwright workflow, and FOUR static guards - check-compose, check-zebrad-config, check-infra-docs and, after F-43-2, check-ledger-structure (eleven in the tree now, with #42's two). FOUR THINGS THE SPEC ASSUMED THAT ZEBRA'S SOURCE CONTRADICTS, each read at tag v6.2.3 rather than inferred: there is NO address-index config key anywhere in `ZebradConfig` - the address RPCs the gateway depends on are unconditional, declared at `methods.rs` 232/438/459, so the requirement is satisfied by the node rather than by a config line; ZEBRAD EXPOSES NO ZMQ SOCKET, so the indexer's polling fallback is not a degraded mode but the only mode, and a silent one - one WARN line at boot and then polling forever; Zebra 6.x HAS an HTTP health server (`/healthy`, `/ready`) the previous compose file could not have used, and the container check uses `/healthy` because `/ready` is false for the days a first sync takes; and the official cloudflared image is DISTROLESS, so nothing inside it can call cloudflared's own `/ready` - which is why there is a fourth Dockerfile rather than a healthcheck that cannot fail. DELIVERABLE 5, the integration-test isolation LEDGER-06 Q6 asked for: SCHEMA-PER-RUN, chosen over an advisory lock (which buys safety by preventing concurrency, and silently loses the lock if a pool reconnects) and over a database per worker (which needs CREATEDB). Two concurrent runs now pass 60/60 with distinct schemas and nothing left behind; without it the same two runs reproduce LEDGER-06 Q6 in BOTH directions - `expected 4 to be +0` and `expected +0 to be 4` - and the test that fails is the four-pool CONSERVATION assertion. The concurrency probe found a second hazard the schema fix does not cover: `migrations.test.ts` creates three scratch DATABASES with fixed names, and databases live in `pg_database`, which is cluster-wide and has no schema to hide in. A2 and A9 are BLOCKED and reported as such with executed transcripts of the refusal rather than as omissions: `docker build` cannot pull base images (403 from the org egress proxy at `production.cloudfront.docker.com`, all four Dockerfiles, BuildKit's parse is the only verification available), and the mainnet fixture needs a synced node no session can reach - the fourth handoff to carry LEDGER-00 Q4. ONE GATE ROUND, 4 lenses, 52 findings, and its verify phase died at a usage limit after ~7: the lead read the other 45 rather than ship them unread, and labelled that weaker evidence. 22 fixed. The runbook as first written COULD NOT BE EXECUTED - compose interpolates the whole file before deciding what to start, so deferring `TUNNEL_TOKEN` to section 6 made `docker compose config` fail in section 1; `openssl rand -base64 32` produces URL-reserved characters that break `DATABASE_URL` about half the time; and the one alert that matters read a `height` field from `/api/pools`, which answers 503 by design, so it would have fired daily from day one and been muted within a week. FOUR GUARDS WERE BLIND: A5 could not see a secret in a `${VAR:-default}` - and closing that flagged a literal password default THIS SESSION had written to satisfy the earlier blindness; A4 accepted compose's two ways of REMOVING a healthcheck; the service splitter went blind on a trailing comment and misfiled that service's lines onto the previous one; and the zebrad cross-file checks were substring searches over a file whose comments quote the very strings being searched for. THE FIX COMMIT CREATED A DEFECT, four handoffs running now: the replacement alert query read `MAX(height)` from a table whose column is `block_height`, caught by executing it against the live schema rather than by reading it. Gate: 1036 passed / 1 skipped, typecheck 10/10, lint 0, eight static guards, build 7/7, `docker compose config` rc=0 on both files. LEDGER-10 asks four questions; Q1 reports that the pinned node is 6.2.3 while `packages/zebra-rpc` was written against 6.3.0's structs, which is a decision rather than a default. Rebased onto 4ae0796 after #42 merged: ci.yml and package.json reconciled to the UNION of ten guards (the five predating both branches, #42's check-audit-consumers and check-finding-sites, and this handoff's check-compose, check-zebrad-config and check-infra-docs), both LOG rows kept. Re-gated after the merge: 1058 passed / 1 skipped rc=0 (content 67, zebra-rpc 38, web 368, gateway 131, indexer 454), typecheck 10/10, lint clean, content validate OK, pnpm check eleven guards, pnpm build 7/7 with 0 cached. The +22 over the pre-#40 1036 is #40's and #41's, already on this branch before today's merge - #42 added no vitest test at all (fourteen files, no `.test.ts`; what it shipped is two guard scripts, which is why the merge moved the guard count 8 -> 10 and not the test count). This handoff adds no vitest test either; its instrument is four self-testing guards. L2's F-43-1 applied as a RULE rather than a fix (LEDGER-06 Q4): three of check-infra-docs' fourteen topic rows could be closed by a sentence, not one, each fixed to a command shape with a two-polarity transcript on one document. F-43-1's own worked example does not reproduce - "before migrating" does not contain "migrate" - reported rather than silently repaired, which is the rule fold 4 adds to CLAUDE.md. A1 re-executed rather than deferred (docker compose config needs no daemon): rc=0 on both files, and Docker's parser independently confirms the healthchecks, the service_healthy edges and the single 0.0.0.0 binding. A2 still BLOCKED but correctly attributed: dockerd 29.3.1 started, registry answers 401, and it is the layer CDN alone that the egress proxy denies. No container started; docker ps -a and docker images both empty. L2 then verified 56779f8 on a clean worktree and returned F-43-2: THE REBASE MERGE HAD CROSSED TWO HEADINGS OVER THEIR CONTENT IN LEDGER.md - `## HANDOFF-10` spliced under HANDOFF-08's closing sentence so it governed HANDOFF-08's own section 8, and `## HANDOFF-08` left with five lines and no block. Nothing lost, everything filed under the wrong name, in the file HANDOFF-09 reads next by handoff number. Mapping the region before touching it found a THIRD defect L2 did not name: the two handoffs' section 8 blocks shared ONE fence with no separator, so moving the heading alone would have left them concatenated. Answered with guard ELEVEN, `check-ledger-structure.mjs`, whose two rules were each measured against the real damaged file before being written (R1 a heading is preceded by a blank line, R2 a heading governs a fenced block - one finding each, at 2674 and 2668, and zero on the repaired file). L2's own proposed rule was measured and REJECTED: it misses F-43-2 at both sites and fires on three correct blocks. Fold 3 reported as impossible rather than forced - check-finding-sites excludes LEDGER.md by construction and self-tests the exclusion, so registering F-43-2 there would be coverage that checks nothing. And L2's MECHANISM for F-43-2 is wrong in the same shape as F-43-1's: ATX headings interrupt paragraphs, so the spliced heading rendered as an h2 on GitHub all along - executed against the reference commonmark parser. It was never invisible; a heading in the wrong place just looks like one in the right place, which is a stronger argument for the guard than the finding gave. |
| 2026-08-30 | 09 instruments + snapshot | [#44](https://github.com/aqua-019/ZCashReveal/pull/44) | shipped (PR opened) | 1 | The three instruments and the publisher. `turnstile-accounting.ts` (plan 3.1-3.3: U, V, the Orchard drain D and velocity, plus a falsifiable exit-only law with its worked case executed by name), `migration-lens.ts` (plan 3.4 / TRACKING-MATH 3.9), `ironwood-birth.ts` (plan 3.5), all pure, all emitting FilterApplication records - three new union variants registered with `check-audit-consumers.mjs` in the same commit that introduced them, which is LEDGER-10 fold 8 - and a seam test that runs each record through the gateway's flatten-and-parse gate with a negative (the residual narrows nothing, so it emits none) and a fail side (a nested params value IS rejected). `apps/publisher` new: SnapshotV1 with four required fields and every panel nullable, a file sink and a redis sink behind one `MULTI`, a file-backed monthly command counter on a named volume, and a config that calls `assertNotManagedStore` on `REDIS_URL` and `DATABASE_URL` but deliberately not on `SNAPSHOT_REDIS_*`, which is this app's destination. Gateway: the 501 stub replaced by a 200 with max-age=60 and a 503 carrying one of four reasons and `no-store`; the snapshot frame now rides the `{channel, payload}` envelope and arrives before the mempool frame. CI gained a `redis:7` service and a publisher step, so A7's integration half runs rather than skipping - and it ran here too, against a real local Redis. L2's nine folds applied in the first commit, with fold 1's stated REASON corrected against Zebra's source at both tags (the 6.3.0 change is recipient names and specification URLs, not addresses - `subsidy.rs` is byte-identical between v6.2.3 and v6.3.0) and fold 3 reported as rejected by its own guard before being resolved with a proof-based exemption. One gate round, 47 raw findings, 12 refuted, 35 confirmed and all 35 dispositioned with the budget stated in the round's first line. One HIGH: the gateway mounted no snapshot volume, so `GET /api/snapshot` would have answered 503 forever on a correctly-running stack - fixed and then guarded, after the guard's FIRST detector was found unable to catch the defect it was written for (it examined only services that SET `SNAPSHOT_FILE`; the defect was a reader setting nothing). One user-visible MEDIUM that is a spec conflict rather than a code slip: `maxWallets` published TRACKING-MATH 3.9's denomination-run count, which two wallets crossing one denomination in adjacent blocks falsify, and which TIGHTENS as evidence accumulates - 847 identical crossings would have claimed 'at most 1 wallet' over 84,700 ZEC. Now plan 3.4's `Sigma counts`, with the run count beside it as `denominationRuns`, swept across seven sites and recorded SPEC-WAS-AMBIGUOUS for L2. Two probes that could not fail, both found and both repaired with their reason: A11's fail side was reaching `snapshotKeyForHeight`'s throw one function earlier and never running the namespace guard at all, and `check-redis-safety` matched `.scan(` while missing `.scanStream(` - an unbounded enumeration of a store shared with another project's production data. One credential leak: the log redaction's password class excluded `/`, which is in base64's alphabet, so an Upstash token went to the log verbatim. And the FIX COMMIT REVIEW found the round's most interesting defect for the third session running - that redaction fix was quadratic, 16.4 seconds on 200,000 characters in a function that runs on error messages. A10 recorded as narrower than its name: three is the write count, five cross the wire, and whether the meter bills `MULTI`/`EXEC` is unreadable from a container whose proxy blocks upstash.com, so both numbers are pinned and reading the console is now a named operator task. 1137 passed / 1 skipped, typecheck 11/11, lint clean, content validate OK, eleven guards, `pnpm build` 8/8. |
| 2026-08-31 | 09a estimator package | [#45](https://github.com/aqua-019/ZCashReveal/pull/45) | shipped (PR opened) | 3 | The three estimators, plus the three pure leaves they import, moved out of `apps/indexer/src/analysis/` into `packages/zec-instruments` (`@zcashreveal/instruments`), imported by both the indexer and the publisher, and the publisher's composition root now passes `REAL_INSTRUMENTS` instead of `NO_INSTRUMENTS`. Five of six moved sources byte-identical, the sixth differing by one import specifier; four of six moved tests byte-identical, two by a specifier. `activation-heights.ts` moved because a dependency-free package cannot reach back into the app it came from and duplicating a consensus height is worse. The barrel re-exports BY NAME, not `export *`, or it would have gained twenty activation-height constants it never had. Twelfth guard `check-instrument-deps.mjs` in `pnpm check` and CI. **THE MOVE UN-NULLS TWO OF THE FOUR PANELS, NOT FOUR** - `residual` and `migrationHist` are measurements; `drain` and `neffSeries` are still null because `readSnapshotInputs` cannot supply their inputs (a block-time column on `pool_snapshots`, and an Ironwood spend source), which is the input layer rather than the packaging and is now HANDOFF-11's, raised as LEDGER-09a Q1. L2's five folds applied in the first commit; fold 5's third clause reported as ALREADY SATISFIED rather than acted on twice, and fold 2's section-4 reference reported as naming the wrong section. Three gate rounds, five workers. Round 1: one HIGH found by the lead outside the fan-out - CI enumerates a test step per package and had none for the new one, so 98 tests would have left CI green while `pnpm -r test` stayed right; second instance of a shape `ci.yml` itself records for zebra-rpc. Round 2, three HIGHs and two of them created by this handoff: the publisher suite was resolving the package to `dist` so every A1 result was about an artefact; one row `migrations_zip318` PERMITS stopped the publisher publishing any document for its 1,152-block window; and a node reporting `chainSupply` without `valuePools` published '100 per cent of supply is verified' as a measurement. **The guard this handoff exists to deliver was the worst-reviewed artefact in the branch** - eleven holes whose self-test certified all of them, including never exercising `zeromq` at all because its one case routed through a name banned at hop one. Round 3 reviewed round 2's fix commit and both its HIGHs were round 2's own fixes: the vitest alias applied to one of two sites, and the rewritten guard recommitting the class of hole it closed. Two malformed probes reported rather than redone. Stopping stated in three parts with the extrapolation, and clause (b) reported UNSATISFIED for the loose-predicate shape, which reached instance six here - two of the three new ones written by the session holding the fold against them - where clause (b) says build a guard and fold 4 says specify it; LEDGER-09a Q2 asks L2 to settle it. 1218 passed / 2 skipped (was 1204 / 2), typecheck 13/13, lint clean, content validate OK, twelve guards, thirteen registered findings, `pnpm build` 9/9. |
| 2026-08-31 | 09b snapshot inputs | [#46](https://github.com/aqua-019/ZCashReveal/pull/46) | shipped (PR opened) | 3 (each reviewing the previous fix commit) | The two missing input SOURCES HANDOFF-09a's principal finding exposed. Migration 005 adds `blocks (height, time_s, hash)` - a TABLE, not a column on `pool_snapshots`, because a block has one time and a column stores that one consensus number four times per height, and because `PoolStateSnapshot` is a shared type with no time field so the column shape forces widening it or breaking the uniform writer signature - plus `pool_nullifiers.anchor_root`, the missing (nullifier -> anchor) edge. `writeBlock` and `writePoolSnapshot` are the first writer `pool_snapshots` has ever had. `readSnapshotInputs` reads both, so `drain` and `neffSeries` are measurements on the production path and all four panels are non-null, asserted by VALUE against a real Postgres 16. **Two premises in the commissioned scope were false**: `pool_nullifiers` has admitted ironwood since 003 widened its CHECK by name (proven against `pg_constraint`, with `'tachyon'` rejected so the constraint is shown live), which changed the deliverable from a `candidate_count` column to the anchor edge; and `pool_snapshots` had no writer at all, so there is nothing to backfill anywhere, ever. A4 is the sharp assertion: block time gives -1,000 ZEC/h and the write clock gives -172,043,100. **Three gate rounds, budgets in every first line (28/24 and 16/14, then 34/24, then 57/44 with ten mutations, nine killing their target and one surviving).** Round 1: the publisher suite was TRUNCATING the shared database, an empty join published `neffSeries` as a measured zero, `readSnapshotInputs` had no try/catch under a docblock that said in capitals that it did, and the three production queries had ZERO execution coverage - breaking all three left the suite green. Round 2: the fix commit had reintroduced the mixed-chain row one table over, and I had edited migration 005's bytes after application - my own rule, one commit after writing it for 003. Round 3: the surviving mutation (`height > 0` rejects genesis and passes 444 tests), a correction landing in one file while the branch's own two restatements stood, and a count in two commit messages that did not reproduce. **The gate has NOT converged and says so**: round 3 returned findings a user could see and its fix commit is unreviewed. Folds 1-6 applied; fold 5 was already satisfied and is reported rather than applied twice; fold 1's stated verification does not discriminate and is corrected in place. 1220 -> 1266 tests, measured per package. |
| 2026-08-31 | 09b snapshot inputs (round 4) | [#46](https://github.com/aqua-019/ZCashReveal/pull/46) | shipped (PR open, gate closed at 7) | 7 | L2 gated PR #46 DO NOT MERGE and reviewed `0e2df0c` as its own commit because nobody had - the review section 7 itself named as missing. **F-46-1 (MEDIUM): round 3's fix corrected the RENDERING layer and left the LOG layer stating the falsehood it removed.** The pre-birth branch returns a measurement - `spends: []` over a real window - and still called `fault("neffSeries", ...)`, whose one production wiring logs at ERROR "an input query failed". Both halves false, on ~3.4 million blocks of an initial sync, demonstrated with a query that throws if called: it never is. Round 3 had PINNED ITS OWN DEFECT as correct behaviour, which is why it survived that round. Fixed as no report at all rather than a quieter channel, argued on four grounds. **And the round-4 fix comment repeated the shape it was fixing** - claiming the condition is readable from a window that is not published - caught by measuring the document rather than re-reading the sentence, with the test carrying the same confusion (asserting on the inputs where the claim is about the document). L2 also records ITS OWN error: the section 1 premise about `pool_nullifiers` was false because it enumerated every CREATE TABLE and migration 003 widened the constraint with an ALTER - yielding the rule that an exhaustive claim is only exhaustive over the thing it enumerates, and the thing to enumerate is the OBJECT rather than a source that constructs it. Four rulings into CLAUDE.md before any work: that rule, every scripted replacement asserting its pattern matched, counting instances against the ORIGIN rather than the face so a shipped guard does not reset the count, and clause (ii) bounded so the fix-commit regress terminates where a fix can no longer carry a behavioural defect. Q4's form-A guard deferred to HANDOFF-12 fold 1 with its three hits named. **THEN THE TWO REVIEWS L2 COMMISSIONED RETURNED, AND BOTH FOUND THE FIX COMMIT** - budgets 34/21 and 22/15, eleven findings, three HIGH. The one worth the ledger: `truncate-guard.test.ts`, written by round 3 to prove the guard protects a developer's database, PERFORMED THE WIPE ITSELF. Its hatch case must delete `ZR_TEST_SCHEMA` to test the hatch, and it then called the real `truncateAll`; `getSql()` fixes `search_path` at creation time, so on any run whose globalSetup line is missing the connection resolves to `public`. Reproduced on a throwaway database: **1 failed / 3 passed with blocks=0 before, 1 failed / 3 passed with blocks=1 after** - the test result is the same line in both polarities, and only the database carries the difference, which is why three rounds of reading missed it. Second HIGH: round 3's commit saying "both sites now guard" had DELETED the only test of one of them, and the first fix used one mutation for both restored halves and killed only the async one - a fail-side probe that does not discriminate, reported rather than repaired. Third: the sweep rule inside the commit that argued for it, section 8.1's contract still naming 09b as owner of an absence 09b closed, 25 lines under the paragraph saying it had. Also: the F-46-1 test asserted in a comment that the query IS called and never checked (mutating the guard to `if (true)` left both halves green), and the deletion's "the document already carries it, at the surface that has readers" named readers that do not exist - `neffSeries` appears zero times in apps/web - fixed by making it true with a four-row rendering contract. Round 3's extrapolation is kept on the record as wrong in the direction that flatters the branch, for the second time on this branch. `923372e` changes control flow in three places, so under the amended clause (ii) it earned round 5. **ROUND 5 IS THE FIRST NEGATIVE RESULT THIS BRANCH HAS HAD**: 47 candidates / 35 by execution - eleven mutations, a 2,000-case randomised production-path sweep, three throwaway-database reproductions, a 63-pair predicate-equivalence check - and **no live defect in any executable line round 4 added**. The producer refusal cannot fire on the production path (1,958 of 2,000 tips published, 0 refusals, and an injected `spendsInWindow: rows.length - 1` fires it at once, so the probe discriminates); `mayTruncate` is exhaustively equal to the condition it replaced. All eight findings are in prose, a guard, or one assertion. Both HIGHs are the same error twice in the runbook section round 4 had just written: its `published?` column said a non-positive drain baseline publishes the series without its baseline when `buildDrain` returns null and the whole panel goes, and the section says "the publisher logs each one" while documenting one of TWO production sinks - the second an inline callback logging "analysis panel refused its inputs", reached by `buildDrain` on an Orchard series emptied by the INNER JOIN against an empty `blocks`, which is any 005 database before a backfill. One cause for both, and the commit message named it wrongly: the table enumerated the `fault()` CALL SITES without following what each RETURNS. The MEDIUM is the same shape one layer down - 8.1's new `neffSeries` string named a rarer cause than the dominant one, so a renderer would say the query failed when it answered and nothing could be bounded. **And the owner-to-condition sweep landed at one site of three**, leaving the sentence introducing the rewritten table still mandating an owner: fifth instance of "a corrected fact landing at some of its sites", the fifth committed inside the fix for the fourth, so it takes the register row `H09b-ABSENCE-CONDITION` driven to FAIL naming both open sites and to PASS at 15 findings / 42 sites. `39de2f6` changes two GUARD PREDICATES and a test assertion, so it is not prose-only and earned round 6. **ROUND 6 FOUND THE GUARD ROUND 5 WROTE TO STOP THIS SHAPE WAS GREEN ON IT**: restoring the round-3 table rows verbatim - the actual forbidden rendering string - left `H09b-ABSENCE-CONDITION` passing, because its fail side had been "revert the two sentences the fix touched" rather than a value from the set the predicate excludes. That is CLAUDE.md's data-mutation rule broken by the session quoting it, and the mechanism is worth the ledger: a guard over a document has a PROSE surface and an OBJECT surface, and reverting the prose feels like a data mutation while testing only the code path. Every register row now carries a `dataProbe` and an arm matching the rendering string rather than the sentence about it; seven of eight paraphrases caught, the eighth recorded as beyond a phrase match. **And the sweep that guard existed to close had landed at three sites of seven** - sixth instance, inside the fix for the fifth - the worst being `HANDOFF-11`'s queued section 3 CONTRACT, which cites SNAPSHOT.md section 8.1 as its authority and hands the cutover session the exact string 8.1 forbids. Amended in place, reversing this session's earlier decision to leave it for L2, on the ground that the bullet itself invokes the LEDGER-10 Q5 precedent and its premise changed when 09b shipped; the cross-handoff prohibition is about SCOPE, not consistency, so `flatten()` now drops struck spans. **Neither guard's self-test iterated its own rule data** (LEDGER-09a Q3) though both had just gained members; wiring it in found three register rows never driven against any text and eleven probe gaps across the runbook topics - written to catch one member, found eleven. Round 5's own CLAUDE.md edit had made `NOT CONVERGING` unreachable, since on a long gate every round after the first is a fix-commit review; narrowed to same-fingerprint repeats. A read-only worker wrote to the tree for the third time in a third role and reported it against itself. Prediction record three rounds running: right about location, wrong about count and severity, always flattering. `9a534ed` changes `flatten()` for every register row rather than the one it was written for, so round 7 was owed. **L2 THEN BLOCKED THE MERGE**, reproducing `H09b-ABSENCE-CONDITION` green on the data it forbids - correct, and against a head two commits behind, since round 6's fix was already pushed; re-executed on `8d0c28d` it FAILS rc=1 in both the hyphen and em-dash forms. L2's sharper half is a correction to this session's own reassurance: **CI catches a guard that FAILS and is silent about one that passes VACUOUSLY**, which is what this one did on the same green run. **ROUND 7 THEN FOUND ROUND 6'S FIX HAD REINTRODUCED THE SAME FAILURE THROUGH A DIFFERENT DOOR**: `~~[\s\S]*?~~` pairs 1-2, 3-4, so ONE stray marker inverts it and the guard eats the complements - 229 characters stripped clean, 16,269 with one stray added, 80.3% of `handoffs/README.md` invisible, and `check-finding-sites.mjs` itself carries five markers. Second HIGH: the widened arm had no polarity and fired on correct statements of its own rule, clearing SNAPSHOT.md's real sentence by FIVE characters in the direction copy-editing moves - a guard that reddens on correct prose is deleted rather than fixed. **L2's item 1 delivered**: the probe loop was under-covering for 7 of 15 rows ("banana" passed every `present`-bearing row while the run printed "self-tested in both directions"), and every row is now driven against its OWN REAL SITES - defect spliced in for `absent` rows, corrected text deleted for `present` rows, because a `present` row cannot be driven by a held string at all. **The probe audit L2 asked for: four of fifteen probes are real-site text, eleven are reconstructions**, with the instrument's lower-bound qualification stated. A seventh sweep site found inside the fix for the sixth (`HANDOFF-09a:210`), and a count this session got wrong and swept to three places (four and seven, not five and eight). Three more malformed probes of my own, reported not redone - eight on the record, the rule has caught every one. **CLAUSE (ii) SECOND AMENDMENT**: once a round returns no finding in an executable line of the product, later rounds review only guard predicates and their self-tests, test assertions, and sentences making a checkable claim about runtime behaviour, checked by EXECUTING it. The product converged at round 5; the gate was reviewing its own output, which is a scope problem rather than a convergence failure. **THE GATE STOPS AT ROUND 7.** 1276 tests measured per package (1273 passed, 3 skipped), sixteen runbook topics, twelve guards, typecheck, lint, build green. PR stops at opened - merging is Aqua's click. |
| 2026-08-31 | 04a legibility + turnstile plane | — | reconcile: 09b closed (PR #46 merged `3225a5c`), 04a written and in-progress | — | HANDOFF-04a written by the session that executes it, from the L2 BRIEF of 31 Aug 2026. First Web handoff written from reader feedback rather than from the plan. 11 gains 04a in depends_on and stays queued. |
| 2026-09-01 | 04a legibility + turnstile plane | PR opened | shipped (PR opened) | 0 (gate is the session's own; no external round) | The legibility pass answering four reader complaints, and the turnstile plane on the splash. **Complaints 1 and 2 closed, 3 closed for HTML text and measured-open for SVG chart text, 4 closed on the splash and open on the other seven Record pages** - stated in section 7.1 because a redesign that reports only what it built has not answered the feedback it was commissioned by. **F-04a-4's premise was false in the branch's favour**: the tree floored at 8.5px, not 10px, with 94 live sub-12px declarations across seven sizes and 24 of them at 9.5px, so the reader's "half the text is basically 9px" was a measurement. Floor set at 12px, seven named rungs in tokens.css, recorded as a ledger question on HANDOFF-01's precedent. **The first map was not monotone and tokens.css claimed it was**: the 11px band went to 13px and landed above the nine sites already at 12px. Every rung was at or above the floor in both versions, so a per-rung check was green on it; the property that caught it quantifies over PAIRS. **A comment cannot fail, and one had been wrong through two handoffs and a design review**: tokens.css stated --ink-faint at 3.05:1 and it is 3.11:1, restated at four sites and swept at all four. Found because A2 computes the ratio from the token rather than reading it off the palette, which is what F-04a-4 asked for and now demonstrates. **F-04a-3 was worse than stated**: nav.ts asserted the invariant in its docblock and carved the exception out of it ninety lines below, arguing from two counts that were both wrong. /pools and /reveal join the bar as UNNUMBERED instrument views, the numbered sequence stays nine and closed, and `check-nav-routes.mjs` is the THIRTEENTH guard - its own fail side found that its first draft had exit 1 unreachable for every input. Count swept at CLAUDE.md, README.md, ci.yml and the R4-GUARDS row. **F-04a-6's second example was not correct**: Cite.tsx carried no digit AND kept confidence, lastVerified and the source list behind the toggle - the collapse rule's own forbidden case. **THE PLANE TAKES A SnapshotV1 AND THAT IS LOAD-BEARING**: the fixture PoolsView carries a per-boundary `flows` field the published document has no field for, so an honest picture would have gone dishonest silently at the cutover. migrationHist measures ONE relation, orchard to ironwood; the other four lanes render "not measured", never "0 crossings". **Two departures from the approved study, both in its own favour**: it prints a measured zero under sprout for a relation its EDGES table does not contain, two lines below its own comment forbidding exactly that; and its shortened retention window is not derivable from a document carrying heights and no per-crossing time, so the window is stated in blocks and the measured count printed beside the drawn one (LEDGER-04a Q2 asks L2 to rule). **SIX DEFECTS FOUND BY RENDERING AND NONE BY READING**: the plane drew nothing (`var(o)` - POOL_SW is a class map, valid CSS resolving to nothing, green on typecheck, lint and build); every label sat a hundred pixels low; Escape set aria-expanded false while the computed rows stayed at 546.844px, so an attribute-only assertion would have passed; the first fix moved that defect rather than closing it; the touch path did not exist because the panel buried the toggle; and sticky hover opened the bar on a device with no hover. **A seventh is still live in the approved study and is reported rather than changed**: 04a-turnstile-plane.html prints a static `PENDING 3` tile beside a legend computing `unconfirmed 0` - F-04a-7's own shape, in the file documenting the fix for it. **SVG text is a different regime, measured**: the loop diagram renders at scale 1.384 at 1440px and 0.72 at 760px and below, so a declared 12 paints at 16.6 and at 8.64 - no declared value satisfies the floor at every width. Two declarations registered below it with that measurement as the reason, iterated by their own tests with a count check so a third cannot ride on the exempted value; the fix is HTML labels over the SVG, which the plane already does. Both design files git mv'd into docs/2.0/mockups/. Thirteen guards, typecheck 13/13, lint 0, content validate and build green. Post-fan-out sweep after all three fan-outs; the second found three stray files and they were the LEAD's, reported by a worker against itself. |
| 2026-09-01 | 04b svg text + record claim order | — | reconcile: 04a closed (PR #47 merged `01abc2e`), 04b written and in-progress | — | HANDOFF-04b written by the session that executes it, from the §1 SCOPE in the L2 RESOLUTION for HANDOFF-04a. **The first handoff whose whole scope is a previous handoff's own §7** - complaint 3 measured-open for SVG chart text, complaint 4 open on seven Record pages. L2's resolution appended verbatim to LEDGER.md; its rule about its own instruments (a probe whose output is an ordering, a pairing or an enumeration is checked against one known member before any finding is built on it) folded into CLAUDE.md. 11 gains 04b in depends_on and stays queued. |
| 2026-09-01 | 04b svg text + record claim order | PR opened | shipped (PR opened) | 0 (the session's own gate; no external round) | The two things 04a measured and did not finish. **The brief understated the defect for the second time running and in the same direction**: L2's Q3 minima (16.10 / 11.11 / 7.94) are the SPLASH plot's three scales exactly - one svg of nine, the site's MAXIMUM among pages carrying SVG text - and the real minima are 5.95 / 3.95 / 4.78 / 2.00 at 1440 / 1024 / 760 / 390. Fourth instance of the enumeration-over-the-wrong-scope shape L2 folded into CLAUDE.md in the same resolution, and L2 corrected itself the same way mid-session. **Two measured facts refute the model everyone was using**: ShieldedShare paints 5.95px on a 1440px DESKTOP because it sits in a 0.8fr column, and it is NON-MONOTONE - 3.95px at 1024, 9.62px at 900 - so every width set samples BOTH SIDES of every breakpoint. **The assertion governed the scope, not the deliverable table** (LEDGER-06 Q4): 25 of the 37 `<text>` elements were outside the four named diagrams and included the three worst on the site, at 2.00 and 2.79px on a phone, in grids whose columns were an INLINE style carrying no collapse. All 37 moved to HTML over the SVG. **The fourteenth guard BANS the construct rather than measuring it** - painted size needs a rendered width that is not statically derivable for two of the nine SVGs - and exit 1 is proven reachable by its first run returning 23 real findings, which is the hole `check-nav-routes.mjs` shipped with in 04a. L2 reached the same conclusion in an interim note while it was being written; both its options were taken. **THE FIX COMMIT WAS THE MOST DANGEROUS COMMIT FOR THE FOURTH SESSION RUNNING**: `calc(<number> + <length>)` is invalid CSS, `--plabel-tx: 0` was a bare number, and **86 of 155 labels resolved `transform: none`** - 34 on charts already converted, screenshotted and called done. F-04a-7's shape one handoff later, green on typecheck, lint, build and the new guard. **Found by reading the CSSOM, not by looking**: a screenshot answers "does this look wrong" and a label 12px from where it belongs does not. Closed by guard R4 plus a rendered transform assertion, both with data-mutation fail sides using the value that shipped. **The fix had its own cost and it was paid**: 254 overlapping label pairs across five routes and twelve widths, answered with TurnstilePlane's own precedent - below 900 the overlay goes and the table twin comes out of `sr-only`, because a table reflows and a viewBox does not - 254 to 0. **Four defects in the checkers and three malformed probes of this session's own**: A4 satisfiable by a digit in a COMMENT; the A3 fail side driving a COPY of the parser; the rendered A4 sweep unable to tell a hidden summary from a digitless one; R3 answering "pass" for a row it could not evaluate because `NaN < floor` is false; a stale server; a rounding error larger than the defect (27,727 false sub-floor nodes, all 11.9x px of 12px text); a pointer parked inside the nav bar in every screenshot; and a sweep measuring every SVG `<text>` twice, once as HTML at scale 1, which is a MASK rather than a duplicate. **Two brief premises false and both checked first**: `Cite.tsx` was ALREADY fixed by 04a and is not the first fix, and 300/520 are element max-widths rather than media breakpoints (sampled anyway, on a better argument). **L2's open question measured**: the /network loop at 390 is a DELIBERATE pan container - `scrollLeft` driven 0 to 362, body `overflow-x: hidden`, zero elements outside a scroller past the viewport. **Claim order closed on all eight Record pages**, with `/sources` and `/method` naming the CONDITION rather than inventing a confidence their schemas do not carry, and `/network` and `/contradictions` choosing the same honesty unprompted. One contract violation fixed in passing: a gold accent on a figure about unprovability. **The fan-out's own verification did not compose** - seven workers, one `.next`, one port - reported rather than absorbed, with every §7 figure re-measured by the lead afterwards. MEASURED: 0 visible SVG text across 15 routes x 10 widths; 0 of 150,590 rendered text nodes below the floor across 13 routes x 12 widths; 659 visible labels, 0 without a transform; 0 collisions at 12 widths; 1351 tests (+5, all apps/web, none fallen); fourteen guards, typecheck, lint, content validate, build green; 182 Playwright tests with both snapshot baselines recaptured. |
| 2026-09-01 | 11 live wiring | [#49](https://github.com/aqua-019/ZCashReveal/pull/49) | shipped (PR opened) | 0 (the session's own gate; no external round) | The cutover, wired. **Deliverable 0 first, and it found three assertions that were GREEN AGAINST THE TREE AND PROVED NOTHING**: the read-write-token grep is not empty and cannot be made empty while its own sibling is satisfied - `playwright.config.ts` blanks that name BECAUSE the sibling requires it - so the pair was jointly unsatisfiable; the SNAPSHOT_REDIS client-bundle sweep passes because there is no reader at all, and the obvious home for one, `src/lib/env.ts`, is three hops inside the client graph; and a fake WebSocket emitting a bare ZecFrame goes green over a live path where every real frame is discarded. Ten assertions changed, each named in section 7 by subject with what it assumed and what stopped being true, including A2's regex - `\d+` cannot match a grouped integer, so it would have PASSED on a fresh site and FAILED at 1,000 blocks behind, the only case it exists for. **TWO DEFECTS NOBODY COMMISSIONED, both live on main, both at a seam between two processes covered by tests on both sides.** Every live WebSocket frame was silently discarded: the gateway has emitted {channel, payload} since HANDOFF-05 and apps/web reads a flat type, so frames went into droppedFrames with no throw - a panel reading 'live' while receiving nothing - and the mismatch was double, the relayed payload carrying `report: LeakReport` where ZecFrame wants `entry: MempoolRow`. And `GET /v2/mempool` answered 500 for every non-empty mempool on a live stack: the indexer writes zatoshi as strings and the gateway CAST rather than revived, so buildMempoolView's first `%` threw. Neither suite could see either, because both built their inputs on the producer's side of the gap. Reproduced in both polarities; `reviveWireZatoshi` checks its own `Zat`-suffix convention by round-tripping five real report shapes. A third in the same family: TipChannelPayload declared a discriminator apps/indexer never sent. **The SnapshotStore**: four rungs, a fault record for every CONFIGURED rung that did not answer (A13's rule that the assertion must fail when the FIRST source is unreachable), a 60 s module-scope memo shared by concurrent callers, server-only and PROVEN so by walking the import graph from every 'use client' entry. `/` and `/pools` gain revalidate = 60, which they never had - which is why A10's read count was zero rather than one. **The epoch clock's fake advance is deleted**: it incremented the height on a local 75 s interval against a fixture, telling every visitor the chain had moved when no block had arrived. `/v2` is the API and `/api` answers 410, swept across 88 sites in 24 live files, with two tests that the prefix rewrite turned into tautologies replaced rather than left green. legacy/dashboard deleted, and the four guards that knew about it updated rather than left to rot. CUTOVER.md, and check-infra-docs extended to cover it with every row driven over the real document. 1,348 -> 1,409 passed, 3 -> 4 skipped (the fourth is A11's live leg, which A11 mandates), 1,413 total; Playwright 192 passed with two visual baselines deliberately recaptured because the system bar changed on every route; fourteen guards, typecheck, lint, content validate and build 8/8 green. LEDGER-11 asks five questions; Q1 reports that a second Playwright production build was written, run and REMOVED because building with a custom distDir makes Next rewrite a tracked tsconfig, after which a clean build fails in a route file nobody touched. |
| 2026-09-01 | 12 runtime poolstate | [#50](https://github.com/aqua-019/ZCashReveal/pull/50) | shipped (PR opened; section 4 deliverables 1-3 NOT delivered) | 0 (no gate was run; section 7 says so rather than implying otherwise) | **Deliverable 0 and the ten folds; the runtime wiring is not started.** Reported as a split because one word would be false either way. **The reconcile found A1 restated AND STILL NOT EXECUTABLE, for a reason L2's reconnaissance did not reach**: `rpcBlockSchema` has parsed `valuePools` since HANDOFF-06, but the `RpcBlock` interface declared it nowhere and `asRpcBlock` forwarded `trees` and not it, so `getBlock()` parsed the node's own per-pool accounting and dropped it one line before the indexer could read it. Fixed, with the test driving the REAL client over a committed capture rather than building an RpcBlock by hand. The six deltas sum to 156,250,000 zat - the subsidy - on both captures exactly, **and only with the LOCKBOX included**: over LedgerLane's five lanes it is short by 18,750,000 every time, pinned as its own test. Fail side is a one-zatoshi data mutation from inside the stated exclusion set. **A3's wiring would have shipped the fourth instance of the seam shape, and the instrument caught it BEFORE the code existed** - the first time on this project that shape was found ahead of the commit rather than one commit later. `serializeReport` stringifies every bigint by value; `reviveWireZatoshi` revives only `/Zat$/`; `ClaimAssessment` carries four bigints with no such suffix. Executed through the real serialiser and the real reviver: **4 of 5 fields returned `string` where the type says `bigint`**, and the `as T` cast means the compiler never objects. **L2's Appendix A carried three defects, all found by executing it**: its self-test drove 2 of the guard's 7 finding arms; `merkleRoot([])` threw, so one malformed capture took the whole run down with a stack trace instead of naming the file; and its docblock claimed the self-test 'drives a known block' while the self-test computed its own root - reversing the byte order and breaking the odd-row duplication BOTH left it green, so a tree with no captures would have shipped either. Now: arms are data, coverage is checked in both directions over `checkOne`'s own source, and a known-answer vector pins the conventions with no fixture present. **Two of L2's five polarity transcripts do not reproduce** - the two committed captures are 9,825 blocks apart so neither has its predecessor and the delta arm cannot run for either; L2's own later paragraph refutes its own table. **F-49-1 needed a THIRD edit L2 did not name**: the guard step was at ci.yml:300 and the zebra-rpc suite at :327, so wiring the report in alone would have had the guard read a file written 27 lines later. **And a fourth**: `zebra-rpc-results.json` was not gitignored - the second instance of a shape `.gitignore`'s own comment already records. **Two guards added, both driven rather than trusted, ten mutations each, all caught by name.** The compose guard has two clauses that CANNOT change a verdict - step 3's anchoring subsumes them - so they are tested by MESSAGE and the header says a green run is not evidence for them; and its comparator's patch term is unreachable against a floor ending in zero, so it is driven against a synthetic 6.3.1 floor. **My own coverage check first had the hole it was written against** (deleting a probe row left it green), and **my section 5 rewrite produced a vacuous pass** by dropping the uppercase EXCLUSION SET marker, so R4 skipped the file and reported OK - caught by the count moving 49/4 to 54/5. **Deliverable 2b is half closed and L2's survey missed it**: all 15 transactions across both captures return OBSERVED, including four v4s - the class where absence is ABSENT_INDETERMINATE - so Zebra emits `vjoinsplit` on the verbosity-2 path. A non-empty JoinSplit stays open. Fold 4's cost stated rather than glossed: `SNAPSHOT_TTL_MS` is 60,000 and was equal to the old `revalidate`, so at 120 s the warm row is a lower bound. Sixteen guards; indexer 457 passed / 0 skipped with a live Postgres; six gates rc=0; `pnpm build` left the tree clean, which is F-49-2 measured from the other side. |
| 2026-09-02 | 12 runtime poolstate (second session) | [#51](https://github.com/aqua-019/ZCashReveal/pull/51) merged `65bdac5` at `5a3893b`; follow-up PR carries the gate fixes | shipped (section 4 deliverables 1-3 DONE) | 3 (round 1: 4 reviewers, 2 returned, 2 died on the account's session limit; 6 findings, all fixed; 2 clusters carried UNVERIFIED. round 2: the lead over the fix commit, 2 findings. round 3: the dispatched reviewer of that fix commit, arriving after the write-back, 4 more - three of them defects the round-1 fix had introduced) | The runtime, built after PR #50 merged PARTIAL and F-50-4 routed the handoff back to `open`. A `PoolState` opens at a BASE from the start block's own figures, so A1 runs live on every block - this build's per-pool delta, balance and commitment count against the node's `valueDeltaZat`, `chainValueZat` and `trees.size`, fatal on disagreement, the block not written. Reorgs roll back to the split and replay fresh (property, 100 runs, plus the worked case). The Ironwood anchor comes from `z_gettreestate`, asked only where the decoder marks it, root from the treestate and maxPosition from the block. Every spend and every link is assessed against the chain state; `UNKNOWN_ANCHOR` carries the Zebra #10461 byte-order diagnostic. The wire seam is tagged by value both ways and proven over the REAL producer's report. **The PR merged mid-session, one commit before the gate's fixes** - main has the runtime and not the six defects round 1 found in it, two of them HIGH; the follow-up PR carries them. |
| 2026-09-02 | 13 mode A plan (PLAN ONLY) | [#53](https://github.com/aqua-019/ZCashReveal/pull/53) **merged by the operator at `ef7312f` mid-gate**, then [#54](https://github.com/aqua-019/ZCashReveal/pull/54) for rounds 2 and 3 | shipped (follow-up PR opened as a draft) | **5 rounds** (round 1: 49 raised / 9 confirmed; round 2: 30 / 18; round 3 the lead's own scoped audit: 4; round 4: 147 agents, **91 killed by a usage limit**, 1 confirmed - and the container then wiped both /tmp and the workflow journal, so **that round's own record no longer exists**, which is not the same as a round that found nothing) (round 1: 7 dimensions dispatched, 6 returned, 45 findings - the guard-count sweep never returned and is reported as UNVERIFIED work; round 2: 4 dimensions over the fix commits, 44 findings; round 3: the lead's own (a)/(b)/(c)-scoped audit of round 2's fix commit, 4 findings, all its own). Every finding reproduced by the lead by execution before acceptance. **NO ROUND CLAIMED CONVERGENT** - clause (i)(a) fails because round 3 returned visible findings, and (i)(b) fails because the prose-against-table shape recurred across three rounds with no guard, a decision recorded as weaker per clause (b) rather than dressed as covered | **A plan that stops at a plan, and three of the brief's premises were false.** `check-compose.mjs` does NOT parse `.env.example` - it reads two compose files and nothing else - so "the reach exists, extend it" was untrue and 0b is a sibling on evidence. **Zebra #10461 does not reverse the transaction-side anchor byte order**: read against the merged diff it PRESERVES the existing reversed display order while re-implementing it, and on the Sapling path the `.reverse()` is an unchanged context line; its second half is correct, the diff touching neither `getblock` nor `z_gettreestate` roots. The ceiling is kept on the LARGER real change - a 106-file transaction-model replacement deleting `sapling_spends_per_anchor()` - and set at 6.3.0 INCLUSIVE because `git tag --contains 1c9b245` is EMPTY against all 147 tags, positively controlled. And `/api/compact/:range` would have answered 410 to its own client. **Two claims in the plan's own first draft were also wrong**: `WebAssembly.Memory.prototype.buffer` hands the whole linear memory to JS, so "no I/O imports" closes only half the claim and the worker's separate realm closes the rest - which then became the strongest argument against the threaded build, since a SHARED memory is exactly the object that crosses that boundary; and `fetch(url, {integrity})` does carry SRI, so the module is integrity-checkable after all. **The gate's worst two findings were in work already believed finished.** Section 3.3's volume measurement counted `CompactTx.actions` and not `ironwoodActions` - the exact failure section 1.4 warns about, one section earlier - missing 10 of 22 shielded items on the DENSEST pool and understating the figure by 48 per cent; and the ceiling guard's message assertion computed its expected value BY CALLING THE FUNCTION UNDER TEST, the precise shape this same branch specifies a guard against, committed inside the fix for the first instance of it. Also corrected: a 150-second block target where Zcash targets 75 (this repo says so in `turnstile-accounting.ts:218`; the fixtures measure 75.36 s), the newest FIXTURE used as the chain tip where the captures record their own tip in `confirmations`, and Q6's stated reason refuted by its own table five lines above. **The guard-count sweep found the guard that exists to prevent it failing to**: R4-GUARDS' `present` was `/fourteen (static )?guards/i` and with `static` optional it was satisfied at CLAUDE.md by ledger PROSE about the guard population, so the row stayed green for two handoffs while three of four asserting sites disagreed. Seventeen guards; `TEST_RC=0` 1400/109/1509 re-executed at `312cad4` with no database in this session and zero test files touched - an earlier draft of this row said 1400/124/1509, which does not sum; typecheck, lint, validate and build green; `-- apps packages` empty. Eleven malformed probes of the lead's own reported rather than redone - two guard mutants killed by the working directory before the self-test could speak, and BOTH survived when re-run correctly; a fixture recount that read `tx.sapling.outputs` and returned zero Sapling while reproducing Orchard and Ironwood exactly (the fields are `vShieldedOutput`/`vShieldedSpend`), where reading the disagreement as a table defect would have deleted a correct column; and an enumeration whose `--include` filters produced a DIFFERENT fourteen that happened to equal the right total, so the count looked confirmed and the membership was wrong. **Round 2 then found A6's exclusion set satisfied by the shipped document - by the very line stating the measurement does not exist - and the obvious tightening does not fix it, because that line contains both "single-threaded" and its n.** Round 3 found four more, all in the commit that fixed the eleven. **Round 4 confirmed one HIGH, reproduced against real docker compose v5.1.1: both `${` pre-checks and the interpolation scan ran on the RAW line, so a trailing comment made the guard MISS three real literal defaults and FAIL one correct build - wrong in both directions, and the env surface caught the same spelling compose missed.** The origin "a value parser is duplicated per surface" has now produced FOUR faces in four commits, each inside the fix for the one before it, every one found by executing a spelling rather than reading code. **Section 5's threat model has failed verification TWICE and has produced zero facts across both attempts** - it is the argument that a viewing key cannot leave the tab, and no refuter has read it. |
| 2026-09-03 | 14 live without a database (rung 1 of 3) | [#56](https://github.com/aqua-019/ZCashReveal/pull/56) | shipped (PR opened as a draft) | **2 rounds, neither finding in an executable line of the product** (round 1, by the lead: a document sentence; round 2, by CI: a guard's allowlist) | **The publisher runs on RPC alone.** `DATABASE_URL` loses its localhost default and becomes optional; `chainAccessFor` replaces the unconditional `postgres(...)` at `index.ts:97` and is the seam A2 watches; `NO_CHAIN_QUERIES` is the RPC-only value. **Three panels null, not four** - `residual` is MEASURED, because `U = Bal^sprout + Bal^orchard` and `Supply` both arrive on `getblockchaininfo`, and the handoff's own brief said "four" twice against its own transcript showing three (corrected in sections 4 and 5 per LEDGER-11 Q5(a), and A1b now asserts the surviving panel positively). **`snapshot age` no longer reports 0 blocks on a twelve-day-old fixture**: `SnapshotAge` is a discriminated result, `EpochClock` tracks `sawTipFrame` as its own state, and four places that encoded the old contract are corrected - `format.test.ts`, `status-affordances.test.tsx`, the e2e spec (which REQUIRED a digit on a fixture build) and the primitives gallery. `RUNTIME.md` gains section 7; `docs/2.0/CUTOVER-1.0.md` is new. **Four of this session's probes were wrong before the code was** and each is recorded in the test rather than quietly repaired: a fail side that removed `chainSupply` and met the documented `valuePools` fallback; a fixture that made the two supply sources equal; `U` asserted as every shielded lane; and an A3 fixture that inherited `residual: null` - which is how the session learned **the committed web fixture renders the site's headline figure as an absence today** (LEDGER-14 Q2). **Gate round 1's one finding was in `CUTOVER-1.0.md`'s rollback section**, which said stopping the publisher lets the site fall back "until its TTL expires" - `zecreveal:snapshot:latest` carries no TTL by design, so an operator would have left a frozen page up believing they had rolled back. Found by executing the sentence rather than reading it, and swept (two other hits, both correct as written). **The live RPC endpoint is unreachable from any session here** - two hosts, both 403 at CONNECT, recorded in the proxy's own failure list - so that half of deliverable 2 ships as `scripts/prove-rpc-only.mjs`, driven in four directions against a local stand-in. 1525 passed / 5 skipped, all five named; `TEST_RC=0 TYPECHECK_RC=0 LINT_RC=0 VALIDATE_RC=0 CHECK_RC=0 BUILD_RC=0`, each read directly from its own process (F-53-1). **Round 2 was CI's**: `assert-no-skipped-integration.mjs` went red on the PR head with 707 tests passed and 0 failed, because the new suite's two `runIf` markers were not on its `ALLOWED_SKIPS` - the fifth face of LEDGER-09b Q3's origin. Fixed by naming them, which is the deliberate edit the guard's own header asks for; discriminated by stopping Redis and watching it catch the REAL test by name. **No local command runs that guard** - it needs vitest JSON reports only `ci.yml` asks for - so all six required gates passed on the commit CI rejected. Second time a gate has existed only in CI (the first was `pnpm build`, HANDOFF-07); recorded as LEDGER-14 Q5 rather than fixed, since emitting JSON locally changes every package's test invocation. |
| 2026-09-04 | 15 live transactions (rung 2 of 3) | [#57](https://github.com/aqua-019/ZCashReveal/pull/57) | closed (merged f976477) | **4 rounds** (round 1, the lead: 1 finding, a live 500; round 2, the lead over that fix: 1; round 3, two dispatched reviewers on FAILURE PATHS: 11, two CRITICAL; round 4, the lead over round 3's fix: 1, a false document table). Every finding reproduced by the lead by execution before acceptance; nothing carried UNVERIFIED | **The mempool path on a rate-limited third-party endpoint.** `RateGate` holds a ROLLING window (a bucket emptying on the minute admits five at 59.9 s and five at 60.1 s, and the measured endpoint refuses on the sixth); `planMempoolPoll` derives the interval and a per-tick budget - at the measured 5/min that is one tick a minute and three transactions. **Deliverable 0 corrected its own brief's section 1**: the loop reads Postgres at `anchor-depth.ts:57` and writes it at `index.ts:254` off a `DATABASE_URL` with a localhost default, which is LEDGER-14's composition-root finding in a SECOND app, so "no database" became deliverable 6 rather than a footnote. **THE FAIL-SIDE RULE TURNED ON ITS AUTHOR**: the 429 mock answered `{error: "rate limited"}` - `error` as a STRING, which fails `envelopeSchema` - so the ONE 429 shape the whole suite drove was the one that dodged the client's error-object branch BY ACCIDENT. A real gateway's envelope became an `RpcError` that never penalised the gate; a Cloudflare HTML page became a bare `Error` retried three times against a five-request minute. Both verbatim the defect `rate-limit.ts`'s own header says it removed, eleven green tests over them. **A 429 on the tick's overhead published NOTHING**, so the gateway re-aged a `complete: true` record forever - /track read "4 of 4 analysed, every transaction analysed, 47 min ago" with the mempool unreachable. **A month-long `Retry-After` SPUN rather than parked**: Node clamps a 2.6e9 ms timer to 1 ms and warns, so `take()` looped at a kilohertz with the non-reentrancy flag held. **`deferred` came from the budget slice before the loop**, so a refusal-first tick of 100 published 97 with three rows in no bucket, and an unmetered tick that analysed two of eight published 0. **THREE SENTENCES WERE TRUE OF NOTHING AND ALL THREE PREDATED THE BEHAVIOUR** - the TTL rationale, the "view is now aging" log, and RUNTIME 8.6's "never to zeros" (false for `severity`, which bottoms out at INFO) - which is clause (c) plus the rollback sub-class adopted from LEDGER-14 Q3 in this session's own second commit. **RUNTIME 8.5's copy table drifted TWICE**, once transcribed and once under a gate fix; a test now reads the document, recorded as WEAKER than a guard. **Five probes of mine were wrong before the code was** and one instrument understated its own finding (`JSON.stringify(NaN)` is `"null"`, so a NaN read as benign). One real defect in code written the same hour: `parseRetryAfterMs("1.5")` returned 0 because `Date.parse("1.5")` is 5 January 2001 - absence rendered as "retry immediately". The live endpoint refused CONNECT with 403 from this container and L2 reached it from another the same day, so the wall is container-scoped and the local mock - a REAL HTTP server, which is what let round 3 find the 429 ordering at all - was the whole harness. 1597 passed / 5 skipped / 1602 total, all five named; degraded polarity 1465 / 111 at the SAME exit code, which is the whole of A6. Seventeen guards, six gates rc=0 each read from its own process, `assert-no-skipped-integration` cleared LOCALLY before the push. |
| 2026-09-04 | 16 crossings forward (rung 3 of 3) | [#58](https://github.com/aqua-019/ZCashReveal/pull/58) | shipped (PR opened as a draft) | **5 rounds** (round 4 = F-52-2's round on `62c4e77`, five reviewers on failure paths, 23 findings; round 5 over round 4's three fix commits, 16 more, two HIGH and one that had UNDONE RUNG 2). Every finding acted on reproduced by the lead by EXECUTION before its fix; two REFUTED by execution. **Round 5's third lane died on a session limit and is reported as UNRUN, not clean** - it was the prose and test-assertion lane, so this session's own sentences are the least-reviewed thing in the branch | **The confirmed-block driver on a third-party endpoint, and three of section 1's own measurements corrected.** `scripts/preflight-rpc.mjs` answers an endpoint by execution in ten seconds - which of the eight wire shapes this stack sends are served, what version answers, and the rate, with THREE outcomes per method because an error about the ARGUMENT proves the method exists and a 429 proves nothing. **THE FINDING OF THE RUNG: a missing `z_gettreestate` did not degrade the driver, it STOPPED it.** `getTreestate` returns a non-nullable promise and `call()` throws `RpcError` for `-32601`; `isFatal` reads false for that, so the loop re-fetched the same block for ever at the first Ironwood height - permanent AND invisible, the combination section 3 names as the worst, written into the brief guarding against it. `TreestateSource`'s contract had documented `null` since HANDOFF-12 and nothing could produce it: **a documented case with no producer is not a case, and its docblock reads exactly like one that works.** **THE FOLLOWER WAS ASKING THIRTY TIMES A MINUTE AGAINST A CEILING OF FIVE** - `planMempoolPoll` got the whole ceiling and the follower got the raw interval, on one shared `RateGate` that holds the ceiling BY SLEEPING, so no request count could see it and what it cost was the mempool tick. **AND CROSSINGS NEED A DATABASE, WHICH THE BRIEF DID NOT KNOW**: the publisher is a separate process and builds `migrationHist` from its own Postgres query - executed, `crossings: []` and `migrationWindow: null` with none - so `INDEXER_CHAIN_STORE=memory` runs the follower and puts nothing on the plane, and `CUTOVER-1.0.md` section 10.1 states both shapes in a table. Section 10.7 states the other half: **at five a minute you can have rung 2 or rung 3, not both** - measured, txBudget 3 alone against 0 with the follower running. Deliverable 1b put `test:e2e` and `assert-no-skipped-integration` on the gate list at the shape's third instance and fixed the order, `build` before `typecheck`. **The version ceiling moved to `version-floor.ts` because the guard that declared it had asked in its own docblock whether it should grow a runtime reader; the preflight is that reader.** **Deliverable 1b gained a FOURTH face mid-session: `post-deploy-smoke.yml` had never passed in 44 runs** - `packageManager: pnpm@9.12.0` with zero `pnpm/action-setup` steps, so `setup-node` died before the smoke step - **and reported `1 skipped` on every PR because its `if:` needs a Production deployment, so five gates read past it. No production deploy of this site has ever been smoke-verified.** Three lines, cause confirmed here before the fix, **and PROVEN by dispatching the workflow rather than assuming it**: run 44 on `main` FAILED, run 45 on this branch under a Preview reported SKIPPED (both this session's own confirmation of L2's two halves), and run 46 with the fix is **the first SUCCESS in the workflow's history and the first time its smoke step has executed** - `post-deploy-smoke: OK - 10 script(s) fetched from https://zcuck.xyz; the fallback marker is present and no managed-store name is`. **Eight gates green: BUILD_RC=0 (first) TEST_RC=0 TYPECHECK_RC=0 LINT_RC=0 CHECK_RC=0 VALIDATE_RC=0 E2E_RC=0 SKIPGUARD_RC=0**, each read from its own process (F-53-1). A8 both polarities, identical exit code: **1676 passed / 5 skipped healthy against 1570 / 111 degraded**. e2e 192 passed in 6.7 minutes. `legibility.spec.ts:718` PASSED here under full-suite parallelism, taking HANDOFF-15's carried-forward observation to one failure against six passes. |
| 2026-09-04 | 17 the living tank (Web track reopens) | [#59](https://github.com/aqua-019/ZCashReveal/pull/59) | shipped (PR opened; round 2's three HIGHs deferred to a follow-up PR by the operator) | **2 rounds** (round 1: two reviewers on separate dimensions, 18 findings, 6 acted on; round 2: the fix commit as its own subject per clause (ii) and F-58-2, run as a SEPARATE run rather than a panel racing the lead - **12 findings, 3 HIGH in executable lines, TWO OF THEM CREATED BY ROUND 1's FIX COMMIT**, which is what clause (ii) exists to catch). Every finding reproduced by the lead by EXECUTION. **Neither clause (i)(a) nor convergence is claimed** | **The turnstile plane subscribes to the live transaction stream.** A mark enters on `tx_added`, leaves on `tx_removed`, keyed by txid, capped at 42 drawn and 250 held, with the socket state and the endpoint's published rate on the page. **Deliverable 0 corrected FIVE rows of its own brief's section 1** by reading the module rather than the citation (F-56-1): `TurnstilePlane` is a SERVER component whose own docblock forbids the `useEffect` the brief asked for, so the marks went into a client island over the server board - which also makes A7 true by construction and keeps the whole figure working with JavaScript off; the browser's frame union is `views.ts:1048`, not the `realtime.ts` lines cited, which are a different seam carrying a `LeakReport` where the browser gets a `MempoolRow`; `lanes` is an unordered set with a legal empty and NO DIRECTION, so direction comes from `class` and an underivable row draws nothing (assertion A8, which the brief did not have); `tx_removed` carries three reasons and only one is `confirmed`; and `subscribeFrames` opens a socket per call, which is why `tip-bus.ts` exists, so the ref-count moved into `frame-bus.ts`. **THREE OF ROUND 1's FINDINGS WERE LIVE ON THE DEPLOYED PAGE.** `DEPLOY-2.0` sets Production and Preview to `snapshot` with no WS URL, so the layer opened the committed `FixtureStream` and drew **ELEVEN MOCKUP ROWS** - txids `ee0119443c`, `c0ffee12d3`, from a file whose header calls them invented - under the bold word "live": the fabricated fish section 3 exists to forbid, arriving through the TRANSPORT in the one configuration no unit test drives. A `migration` row drew an orchard-to-ironwood arc for a Sapling-to-Orchard transfer, because the gateway's `crossesWithNoPublicSide` assigns that class to any pool crossing with no public side - **F-57-1, the exclusion set closed by enumeration from a corpus whose only migration row is `O to I` instead of by capture from the producer**. And the pool labels were pushed 117.7 px below the plane onto the caption, verbatim the defect `TurnstilePlane`'s own comment records, one container down. **`subscribeFrames` never passed `onState`**, so the socket's state machine terminated at an undefined callback and the page latched at "live" over a dead feed for ever - and A6's own pass state drove the test hook, so its green run was evidence about the hook, **F-58-1 catching this session one commit after it adopted F-58-1**. `snapshot` was folded additively, so a reconnect kept confirmed transactions on the board: two consumers of one frame disagreeing about its meaning, each with passing tests. The plural fix landed in one file while `mempool-summary.ts` still said "1 requests a minute" - HIGH by the sweep rule. **Six of my own probes were wrong before the code was and one mutation was inert**, all recorded in the tests: "three subscribers, one transport" inferred the socket count from frame agreement and stayed green against a socket-per-subscriber mutation; the re-delivery test held exactly `SPLASH_N_MAX`, the one size at which the aggregate cannot move. A4's first run found eleven marks that were REAL FRAMES - checking the probe before judging the code kept a correct assertion from being fixed into a wrong one. I also measured a stale artifact twice from an orphaned `next start` still bound to the port. Bundle measured before and after: `/` 1.9 -> 4.88 kB route JS, 107 -> 117 kB first load. 1745 passed / 5 skipped, all five named; eight gates each read from its own process with `build` first. |
| 2026-09-04 | 18 the round-2 debt (Web) | [#60](https://github.com/aqua-019/ZCashReveal/pull/60) | shipped (PR opened as a draft) | **1 round, two reviewers on separate dimensions over the pinned fix commit (F-58-2), sixteen findings, fifteen settled by EXECUTION; two found independently by BOTH reviewers. FOUR were defects this branch's own round-1 fix commit created.** Neither clause (i)(a) nor convergence is claimed | **HANDOFF-17's named debt, closed - and the first handoff in this directory opened by another handoff's written-down debt.** All three HIGHs reproduced against the PRE-FIX tree by execution before any fix: 300 rows and one reconnect drew txids 259..300 before and 9..50 after, **0 of 42 surviving**, while holding txid(1) - a row the reader had already EVICTED - which is the promotion half the symptom sentence does not carry; `flow="I to O"` drew orchard-to-ironwood; 3,000 undecodable rows gave `held=250 drawn=0 capped=false` and the page printed "of 250 held". **R2-1's real cause is stronger than the brief stated: captured from the real `mempoolRow`, BOTH directions of the ZIP 318 crossing emit `lanes: ["orchard","ironwood"]` - the same array in the same canonical order - so no predicate over `lanes` could ever have worked**, and the direction now comes from `flow`, with the letters declared once in `@zcashreveal/types` and a test holding the browser's local inverse to them by iterating that declaration's own keys. **The local copy is a MEASURED trade, not a preference**: importing the shared object by value pulls zod through the barrel and cost the splash route 21.4 kB / 133 kB against 5.5 kB / 118 kB, A/B'd on one variable. **A FOURTH DEFECT WAS FOUND BY WRITING THE ASSERTION THE BRIEF SAID WAS MISSING**: `frame-bus` delivers every frame to every subscriber regardless of `openInFixture`, by design, so that guard protected the plane only while nothing else on the page opened a socket - one ordinary consumer beside it put **ELEVEN MOCKUP ROWS** on the board, the figure a gate reviewer measured on the deployed page, reached by another route. **The old A15 file passes 19/19 against the exact code that shipped that defect; the new one fails 3.** Gate round 1 then found four more the round-1 fix had created, two of them HIGH: the new CUTOVER checklist item could not fail on the deployment that runbook produces (`grep -c NEXT_PUBLIC` is **0**, so an operator would tick it having verified nothing - LEDGER-14 Q3's shape written into the checklist meant to prevent one), and `capped` folded in the sticky `holdCapped` so "more transactions are in the pool" stood over an EMPTY tank beside "at least 0 held". Also: the dispatch guard landed on `onFrame` and not `onState`, so the same second consumer turned "no live mempool feed is configured" into "no feed replaying the committed corpus"; and the docblock claimed the producer emits two undecidable captions when the literal `"migration"` has NO PRODUCER - the two predicates are exact negations - which is **F-58-1 arriving inside the fix that cites F-58-1**. **The RENDERED figure was asserted nowhere**: reverting the `"at least "` restored R2-3 on the page with 604/604 green. **One finding is ANSWERED rather than fixed** - `replace()`'s duplicate-txid branch is an EQUIVALENT MUTANT, and the lead's first two probes for it both failed to discriminate, which is recorded as a finding about the instruments rather than repaired quietly. Three of the lead's own instruments failed and all three are reported: a mutation harness that lost its working directory TWICE (printing nothing for seven mutants, indistinguishable from seven that kill nothing), `pnpm build` refusing the seam test where vitest accepted it (`apps/gateway`'s `rootDir` forbids importing `apps/web`; the failed build also emitted four stray artifacts the sweep removed), and a seam probe dying in the fixture because `"oi"` is not hex. Eight gates green, each read from its own process with build FIRST; **1781 passed / 5 skipped healthy against 1675 / 126 degraded at the same exit code**; e2e 192 passed on a quiet tree; `assert-no-skipped-integration` cleared locally (835 total, 5 skipped, 16 integration files). |
