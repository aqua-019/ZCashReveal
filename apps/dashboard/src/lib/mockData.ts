/**
 * Mock fixtures used when VITE_MOCK_MODE=true. Replaced with realistic
 * sample reports in a later prompt; this stub keeps the build green.
 */

import type { RawReport } from "../hooks/useMempool";

export const MOCK_REPORTS: RawReport[] = [];

export const MOCK_TIP_HEIGHT: number | null = null;
