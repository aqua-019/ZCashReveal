node_modules/
dist/
build/
.next/
.turbo/
*.log
*.tsbuildinfo
# Every .env variant, not just two. The managed store's READ-WRITE token would live
# in one of these, and .env.production / .env.development.local were committable
# before this widened. docs/2.0/SNAPSHOT.md.
.env*
!.env.example
!apps/web/.env.example
.DS_Store
coverage/
.vscode/
.idea/
zebrad-data/
postgres-data/
redis-data/
.claude/settings.local.json
.claude/*.local.*
docker-compose.override.yml

# vitest JSON report consumed by scripts/assert-no-skipped-integration.mjs in CI
indexer-results.json

# Playwright run artefacts (apps/web). The suite is reproducible; its output is not committed.
test-results/
playwright-report/
